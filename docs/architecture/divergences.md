> Verified against: b2b31381 (2026-08-14)

Living registry of deliberate Rust-vs-Python divergences; entry numbers are stable and referenced by tests.

# Rust port: deliberate divergences from the Python daemon

Every place where the Rust supervisor daemon (rust/crates/supervisor-daemon)
deliberately behaves differently from the Python daemon
(src/aleph/vm/supervisor/daemon.py and the modules it serves from). The Python
daemon remains the parity oracle for everything not listed here; anything else
that differs is a bug in the port. Dispositions: **keep** (intentional,
permanent), **fix-in-python-later** (the Python side has the bug and should
adopt the Rust behavior), **fix-in-rust** (a known Rust gap with a planned
increment) or **fix-in-both-later** (a shared wart neither side should keep).

| # | Divergence | Why | Disposition |
|---|------------|-----|-------------|
| 1 | Socket hardening: the Unix socket is bound under umask 0o077 and chmod 0700 afterwards as a backstop. Python binds with default permissions. | Day-one hardening (design doc section 3): only root, the agent's user, may connect; the socket is never observable with permissive modes. | keep |
| 2 | Identity-checked unlink at shutdown: the daemon records (st_dev, st_ino) of the socket it bound and only unlinks that inode. Python unlinks the path unconditionally and can remove a socket a newer daemon instance re-bound at the same path. | Same bug ported nowhere: fixed on the Rust side. The pre-bind stale-socket unlink stays unconditional, matching Python. | fix-in-python-later |
| 3 | No automatic .env loading. Configuration comes from the process environment; under systemd the unit's EnvironmentFile= provides it, for dev runs the explicit --env-file flag does. | Design doc section 6: implicit dotenv loading makes the effective configuration depend on the working directory. | keep |
| 4 | Empty-string ALEPH_VM_* path variables (SUPERVISOR_GRPC_SOCKET, PERSISTENT_VOLUMES_DIR) fall back to their defaults. Python turns "" into PosixPath('.') and then crashes or measures the current working directory. | An empty variable is an unset variable, not a request to operate on CWD. | keep |
| 5 | GPU inventory ordering is deterministic (lspci order, duplicates removed). Python builds the inventory via a set, so its order is nondeterministic. | The agent treats the inventory as a set; deterministic order costs nothing and makes byte-level comparisons and logs stable. | keep |
| 6 | JSON field whitespace in gpu_inventory_json / available_gpus_json / RecreateNetwork.summary_json is compact (serde_json default) where Python json.dumps uses ", " and ": " separators; summary_json keys also serialize alphabetically instead of insertion order. | Semantically identical payloads; consumers parse the JSON, nobody compares raw bytes. | keep |
| 7 | HostInfo proto fields the Python daemon never fills (cpu_architecture, cpu_vendor, cpu_model, frequencies, memory type, NUMA topology, the narrow gpus list, the SEV/TDX flags) stay at proto defaults in Rust too. | The proto promises more than either daemon delivers today; inventing values would diverge from the oracle. | keep |
| 8 | Ported warts kept bug-for-bug: a vfio-bound GPU from a non-whitelisted vendor aborts startup, and one lspci -nnk subprocess runs per PCI device before the class filter. | Parity first: fixing these on one side only would make conformance runs diverge. | fix-in-python-later |
| 9 | Increment staging (not a divergence, recorded so nobody is surprised): increment 3 landed settings.check() (the /dev/kvm, hypervisor-binary, socket-length and tool preconditions), the pool.setup() counterparts (store schema, forwarding sysctls, base nftables ruleset) and the boot-time nftables/ndppd reconcile (adoption step 5), plus the persistent lifecycle, port-forward mutations and RecreateNetwork. Increment 4 landed ephemeral Firecracker programs (CreateVm with the jailer/vsock launcher, delete/reboot/reinstall, the stop/start NotImplemented pair), RunProgramCode, WatchEvents, StreamLogs and the reattach retry loop. Increment 5 landed the backup surface (StartBackup/GetBackupStatus/ListBackups/DownloadBackup/DeleteBackup/RestoreBackup/RestoreFromImage: the qemu-img convert/check/info seam, tar + sha256 + meta.json archives, the background run and the restore swap). Increment 6 landed the confidential surface (InitializeConfidential, GetMeasurement, InjectSecret) plus confidential CreateVm (the QemuConfidentialVMConfiguration writer and the awaiting-init no-start path) and the ENABLE_CONFIDENTIAL_COMPUTING settings.check() block. The ONLY remaining UNIMPLEMENTED surface is persistent Firecracker programs (entry 39). | Each check and mutation lands with the increment that depends on it. | keep |
| 10 | Shutdown: the 5 second grace for in-flight RPCs matches Python's server.stop(grace=5). A second SIGTERM/SIGINT skips the grace, unlinks the socket (identity-checked) and exits immediately; Python has no second-signal path. | Dev convenience on the Rust side; harmless under systemd, which sends one SIGTERM. | keep |
| 11 | Stopped-but-defined VMs are reported. A controller config whose unit is not active stays in the world view and is served as STOPPED (stopped_at = the adoption instant, other stage timestamps 0), visible to GetVm/GetVmSpec/ListVms and counted in Health.vm_count. The restarted Python daemon instead stops and disables the unit and deletes the config at startup (load_persistent_executions + _cleanup_orphan_resources), which also destroys confidential VMs still awaiting their owner's initialization; it then answers NOT_FOUND for them. Duplicates get the same read-only treatment: when two ACTIVE configs claim the same vm_index, only the first (sorted file order) is adopted, mirroring Python's claimed_vm_ids guard; the others are not served (NOT_FOUND, absent from ListVms and vm_count), where Python also stops and destroys them. The Rust daemon does not adopt, but never destroys. DECIDED with increment 3: the destructive startup sweep (stop/disable dead controllers, delete their configs, tear down orphan chains and taps) is RETIRED, not ported. Adopted-stopped entries are real VMs: StartVm boots them back (the proto's documented StopVm/StartVm contract, which the restarted Python daemon breaks by deleting the definition) and DeleteVm releases them. Hidden VMs (failed adoption) keep their vm_index claims out of the allocator, and DeleteVm honors deletes of hidden VMs by stopping the untracked controller and removing its definition (the discard_failed_reattach port), so nothing becomes unmanageable. A create that failed to boot leaves a stopped-but-defined VM on the next Rust boot where restarted Python would have swept it. | A daemon that adopts must not destroy state; the sweep also destroyed confidential VMs awaiting owner initialization (fixed Python-side by #966 only for that case). | fix-in-python-later |
| 12 | An unparseable controller config file is logged and skipped (the VM is left alone and hidden, like a Python failed reattach). The Python daemon lets the pydantic ValidationError escape load_persistent_executions and aborts startup, crash-looping until an operator removes the file. | The 1.13.0 crash-loop lesson (PR #1016) applied to the one case Python still aborts on. | fix-in-python-later |
| 13 | Graceful degradation instead of startup aborts: with the D-Bus system bus unreachable the daemon still boots and logs, where the Python daemon dies in SystemDManager._connect after 3 attempts. A bus failure at adoption stamps NOTHING: unit states are unknown, so no VM gets stopped_at (or started_at, IPs, port forwards), and each VM's status defers to the live per-RPC unit queries until the bus answers ("bus unreachable" is distinguished from "bus answered, unit inactive" in the unit-state source's error taxonomy). Per-RPC bus failures degrade to all-inactive in both daemons. A per-VM IP-derivation failure at adoption (including invalid IPv4/IPv6 pool settings, which fail every running VM) HIDES the VM with a WARN, matching the restarted Python daemon, which parks failed reattaches in a retry queue and answers NOT_FOUND for them; the Python Network constructor additionally aborts the whole daemon on invalid pool settings, which the Rust daemon never does. D-Bus calls carry a Rust-only 5 second method timeout so a wedged (not just absent) bus also degrades instead of hanging boot or an RPC. Entries adopted under a bus outage carry no derived IPs, which entry 24 covers. | The daemon must boot in containers and stripped environments; a read-only reporter has no safe reason to refuse service, and a transient bus outage at boot must not permanently misreport every VM as stopped. | keep |
| 14 | GetHostInfo.available_gpus_json excludes every GPU referenced by a world-view controller config. Python originally never rebuilt VmExecution.gpus on reattach (adopted VMs' GPUs were reported available, inviting double-attachment); fixed by #1023, which rebuilds the GPU list for VMs adopted running. CLOSED with increment 3: VmInfo.gpus is rebuilt from the lspci inventory for VMs adopted running (inventory cards keep device_id and the hardware x-vga flag; unknown cards are recorded bare from the config, the `_rebuild_reattached_gpus` behavior) and set from the validated request at create. Adopted-stopped entries (which Python destroys, entry 11) report no attachments until StartVm. | Reporting an attached GPU as available invites double-attachment after every supervisor restart; the controller config knows the attachments. | done |
| 15 | GetLogs reads journald through a `journalctl -o json` subprocess; Python uses the native sd-journal binding. Same identifier matches (`vm-{hash}-stdout`/`-stderr`, OR-ed), same journal order, same microsecond timestamps. | No C dependency in the daemon, and the daemon sees exactly the view operators debug with. One subprocess per GetLogs call is fine for an operator-frequency RPC. | keep |
| 16 | GetLogs applies a Rust-only server cap of 10000 lines when max_lines is 0 (`journalctl -n 10000`, i.e. the LAST 10000 entries), and passes `-n max_lines` for tail requests, so the subprocess output is bounded at the source. The proto already documents max_lines 0 as "unlimited (subject to server cap)"; Python has no cap and buffers the whole journal history in memory. Head reads (from_tail=false with max_lines > 0) cannot use `-n` (it keeps the wrong end) and still slice after parsing, exactly like Python's `chunks[:max_lines]`. | Unbounded journal output is a memory DoS on the daemon; the Python side should adopt the same cap. Ordering semantics versus Python are unchanged. | fix-in-python-later |
| 17 | A negative vm_index hides the VM at adoption (IP derivation fails, the VM is skipped with a WARN, see entry 13). Python's `list(subnets)[vm_index]` instead serves the pool's LAST subnet for vm_index -1 (Python negative indexing), silently aliasing a valid subnet. | Deliberately NOT ported: serving the last subnet for a corrupt index is an accident of Python list indexing, not a design; two VMs could silently share a subnet. | keep |
| 18 | The cloud-init NoCloud seed is emitted as `#cloud-config` plus a JSON body (and a JSON network-config), where Python emits yaml.safe_dump output. JSON is a YAML subset, so cloud-init parses both into the same mapping; the parsed structures are asserted equal against the actual Python builders in tests/conformance/test_cloudinit_parity.py. The metadata file was already JSON in Python and stays byte-identical for ASCII hostnames; a non-ASCII hostname serializes as raw UTF-8 here where json.dumps escapes it as \uXXXX (same parsed value, entry 33). | Byte-matching PyYAML's line wrapping and quoting from Rust is fragile for zero benefit; the guest consumes the parsed mapping. | keep |
| 19 | settings.check() gating: the NETWORK_INTERFACE existence check and the ndppd availability check only apply when ALLOW_VM_NETWORKING is on; Python asserts both unconditionally (a daemon with networking disabled would still refuse to boot without a default route or ndppd). Everything else in the ported check() slice is as-is, including the /dev/kvm, hypervisor-binary and 108-byte socket-path aborts, plus (since the increment 3 review batch) the static-policy IPV6_SUBNET_PREFIX >= 124 abort of StaticIPv6Allocator. Deliberately NOT ported from conf.py:506-559: the CONNECTOR_URL scheme assert and the FAKE_DATA_* file asserts (agent-side settings the daemon does not model; they gate agent behavior, not the hypervisor surface). CLOSED with increment 6: the ENABLE_CONFIDENTIAL_COMPUTING block IS ported (SEV_CTL_PATH file check, check_amd_sev_supported / check_amd_sev_es_supported reading /sys/module/kvm_amd/parameters/sev[_es] plus /dev/sev, gated on the flag; SEV-SNP stays commented out on both sides). | A daemon that cannot create taps has no use for either; requiring them would break the container/CI boots increment 2 deliberately supports (entry 13's spirit). Agent-side checks stay out; the confidential check landed with the surface that needs it. | keep |
| 20 | Failing to enable the ip_forward/IPv6-forwarding sysctls at boot logs an ERROR and continues; the Python `Network.setup()` raises and kills the daemon (PermissionError without CAP_NET_ADMIN). The write gate itself is Python-identical: "1" is written only when the current value parses as int 0, so a host running with the sysctl at "2" (IPv6 forwarding with RA acceptance) is left alone, never clobbered to "1". An unparseable current value logs and continues where Python's int() would raise and kill the daemon. On a real node running as root with 0/1 values, behavior is identical. | Unprivileged and containerized boots (conformance CI, dev runs) must still serve; VMs on such a host lack connectivity either way. | keep |
| 21 | Mutations serialize on real locks: CreateVm under a daemon-wide creation lock (held across the boot wait, like Python's `creation_lock`) plus a per-VM lock that every other mutation (stop/start/reboot/reinstall/delete/port forwards) also takes, plus (since the increment 3 review batch) one coarse host-network lock over host-port allocation-through-persistence, tap/nftables setup and teardown, and RecreateNetwork's flush-and-rebuild. Lock order: creation_lock, then a vm_lock, then net_lock (innermost, never held across the systemd waits). Python only holds `creation_lock` and otherwise relies on event-loop interleaving, so two concurrent mutations of the same VM can interleave their steps there. Increment 4 addition: RunProgramCode briefly takes the vm_lock as its `becomes_ready` stand-in (CreateVm holds it through the boot) and releases it before the guest exchange, so a long program request never blocks mutations. | Blocking threads have no cooperative scheduling to lean on; serializing per VM (and serializing host-network mutations, which Python's event loop makes atomic for free) is strictly safer, and the agent serializes per VM anyway. | keep |
| 22 | initialize_nftables failure at boot: the daemon logs an ERROR and serves (a later RecreateNetwork can rebuild the base ruleset); the Python daemon dies inside `Network.setup()` and crash-loops until an operator fixes the host. Per-RPC nftables failures behave identically in both. | The graceful-degradation family (entries 13 and 20): a daemon that must adopt live VMs cannot refuse service over a rebuildable ruleset. | keep |
| 23 | CLOSED with increment 4: the reattach retry loop is ported (pool.py run_reattach_retry_loop, 30s interval, 5 total attempts, exhaustion leaves the live controller alone). Hidden VMs (failed adoption or boot reconcile) are queued; each pass runs under creation_lock with the Python liveness gate (a positively dead controller is stopped/disabled and dequeued; unknown/transitional states spend an attempt). One deliberate residue of entry 11's no-destruction rule: where Python's dead-controller cleanup relies on the next startup sweep to delete the config file, the Rust daemon keeps the file, so the VM is adopted STOPPED on the next boot instead of vanishing. The retry restore re-reads the config from disk where Python retries the snapshot captured at startup (fresher, not weaker). | The loop is background machinery that landed with increment 4's stream plumbing, as planned. | done |
| 24 | CLOSED with increment 4: RecreateNetwork rederives missing IP assignments (from vm_index and vm_hash, both known) before its running filter and stores them on the entry, so a node whose D-Bus was down at daemon boot heals its VM chains through the RPC; the persisted port redirects come back through the existing step 5 reload. | Rederiving lazily closed the operational hazard the entry recorded. | done |
| 25 | Python's `is_service_active` (systemd.py:196-199) returns False for a unit that is active but not enabled (it checks enablement first): a CreateVm retry on such a VM answers INTERNAL (readopt refuses) and RecreateNetwork skips the VM. The Rust daemon reads the raw ActiveState, so active-but-disabled units count as running. | The enablement check in an activity probe is an accident; the raw ActiveState is the truth. | fix-in-python-later |
| 26 | Non-persistent QEMU CreateVm: refused up front (INTERNAL, zero side effects). Python executes the whole create and dies on `AlephQemuInstance.start()`'s NotImplementedError with an empty message, leaving the controller config, the cloud-init image and sometimes tap/chains behind. | The Python wart is destructive residue from a request that can never succeed; matching it would manufacture garbage on disk. | keep |
| 27 | ALLOW_VM_NETWORKING flipped off across a restart with persisted forwards: the Rust daemon adopts the VM and serves it RUNNING (with no IPs and no networking mutations); restarted Python hides it (the reattach dies rebuilding the tap). | A reachable-but-unnetworked VM is more honest than NOT_FOUND for a VM whose qemu still runs. | keep |
| 28 | A broken port-mapping store at adoption (unreadable database, corrupt table): the Rust daemon adopts the VM with zero forwards and a WARN; Python's reattach dies on the store read and hides the VM. | Same family as entry 27: the VM exists and runs; losing the forwards list is strictly less wrong than pretending the VM is gone. | keep |
| 29 | A mid-flight port-mutation failure (nft error halfway through an Add/RemovePortForward): Python leaves the partially-mutated mapped_ports dict visible; the Rust daemon writes the in-memory list only after the full diff succeeds, so readers see the pre-call snapshot. | Snapshot semantics are strictly safer; the store is only written on success in both daemons. | keep |
| 30 | CreateVm's readopt of a live untracked controller registers the world entry BEFORE restoring the network (a failure removes it again); Python registers the execution last. A concurrent GetVm can see the VM a moment earlier here. | The window is read-only and the failure path removes the entry; matching Python would need entry-less restore plumbing for no observable gain. | keep |
| 31 | D-Bus calls retry once on a fresh connection inside the same RPC (the cached connection may have died with a bus restart); Python's per-call failure propagates and only reconnects on the NEXT call. | One transparent retry absorbs bus restarts that Python surfaces as one spurious RPC failure. | keep |
| 32 | Crafted-state numeric/ruleset edges, grouped: a controller config whose mem_size_mb is a float string fails the whole config parse (Python's int() coercion raises the same way, but pydantic's error context differs); non-scalar nftables match predicates (dicts/lists where scalars are expected) are treated as non-matching instead of raising; host_port > 65535 in a crafted store row fails the bind probe (Python's socket.bind raises OverflowError -> caught as unavailable, same outcome); partially-overlapping nft output entries dedup by the superset rule identically. | Hostile-state edges where both daemons refuse service safely; byte-identical error text is not a goal (entry 33). | keep |
| 33 | Cosmetics, grouped: internal error message TEXTS may differ where no test pins them (the pinned ones are byte-identical); non-ASCII JSON string escaping (serde_json raw UTF-8 vs json.dumps \uXXXX, narrowing entry 18's metadata claim); the EXECUTION_LOG_ENABLED stop-time execution-record dump (_save_execution_data) is not ported (agent-side diagnostics of the pre-split layout); ndppd restart debounce coalescing may order the interface stanzas of one burst differently than Python's await interleaving; the 108-byte socket-path check counts bytes here and characters (len(str)) in Python, diverging only for non-ASCII EXECUTION_ROOT paths where the byte count is the kernel truth. | No consumer parses these surfaces; each would cost real complexity to match byte-for-byte. | keep |
| 34 | Joint hardening gap: neither daemon validates the SHAPE of vm_id (64-hex ItemHash). A crafted vm_id containing path separators reaches path joins (controller config, cloud-init seed, socket paths) in both daemons, so path traversal via vm_id is possible in both; the gRPC socket is root-only (0700), which is the current mitigation. Increment 3 deliberately did NOT add Rust-side validation (parity first: Python has none, and a stricter Rust daemon would refuse requests Python accepts). | Validate in both daemons together (one shared contract), with the agent's ItemHash typing as the model. | fix-in-both-later |
| 35 | Stop/teardown gating: Python creates the tap and chains for EVERY spec when networking is on (pool.create_vm_from_spec ignores internet_access) but gates teardown on `enable_networking` = spec.network.internet_access, so stopping an internet_access=false VM LEAKS its tap and chains for the life of the daemon (a restart heals it: the reattach rederives internet_access from the config's interface_name, which is always set). The Rust daemon gates teardown on the config's interface_name and cleans up. | The Python wart is a genuine resource leak (a dead VM's tap device and nftables chains linger); the Rust cleanup is correct. | fix-in-python-later |
| 36 | Shared wart, kept bug-for-bug: RebootVm on a STOPPED persistent VM restarts the controller unit without recreating the tap/chains and without clearing stopped_at/stopping_at or reloading the persisted port mappings (local.py:536-551; the Rust reboot stamps identically, pinned by a cargo test). The VM boots into a missing tap and keeps reporting STOPPED. | Both daemons should either refuse (the StartVm contract covers this) or run the full start path; fixing one side alone would diverge the conformance runs. | fix-in-both-later |
| 37 | StreamLogs serves the documented proto contract instead of the Python accidents. Python's live phase (`make_logs_queue`) starts a journald reader at the HEAD, so include_history=false still replays the entire history (the proto says "only new lines from now") and include_history=true delivers the history TWICE (once via `_history_chunks` with journal timestamps, again via the queue with `time.time_ns()` capture stamps that make old lines look current); the replay is also unbounded. The Rust daemon runs ONE `journalctl --follow` per stream: `--lines=10000` (the entry-16 server cap) for include_history=true (single delivery, gap-free history-to-live transition, journal timestamps throughout), `--lines=0` for false. Unknown/deleted VMs keep the Python shape exactly: optional capped history, then the stream ends, never NOT_FOUND. | The double delivery, the contract-violating full replay and the wall-clock restamping of replayed lines are accidents of the queue implementation, not behavior any client can sanely depend on; the memory bound extends entry 16. | fix-in-python-later |
| 38 | RunProgramCode passes the scope msgpack through opaquely: the RunCodePayload map is assembled by prefixing the fixmap/fixstr header to the request's own `scope_msgpack` bytes, where Python decodes and re-encodes (`msgpack.unpackb` + `RunCodePayload(scope=scope).as_msgpack()`). The parsed payload is identical; the raw bytes differ only when a client's encoder deviates from msgpack defaults (e.g. str/bin type choices), where Python normalizes and Rust preserves. NARROWED by the increment 4 review batch: the scope is now shape-validated up front like grpc_server.py:158 (one whole msgpack value, `strict_map_key` str/bin keys, before the VM lookup, so an invalid scope aborts INTERNAL without touching the VM even for unknown vm_ids), but the validated ORIGINAL bytes are still forwarded, never re-encoded. One residual lenience: rmpv decodes the reserved marker 0xc1 as nil where msgpack-python raises, so a scope containing 0xc1 is forwarded here and aborts INTERNAL there (crafted-input edge). | The design doc pins "msgpack payloads pass through opaque"; validating the shape without a decode/re-encode round trip keeps both the Python abort semantics and the byte-preserving forward. | keep |
| 39 | Persistent Firecracker programs: CreateVm(FIRECRACKER, persistent=true) answers UNIMPLEMENTED; Python boots them under a systemd controller unit (VMConfiguration controller config + wait_for_init). Note neither daemon can re-adopt one after a restart: `hypervisor: firecracker` configs fail the Python reattach and are skipped by the Rust adoption alike. | Increment 4's scope is the ephemeral launcher (design doc row 4); the persistent-program path is controller-unit machinery that belongs with the controller port. | fix-in-rust (controller port) |
| 40 | Block-device rootfs for ephemeral programs (Python's `enable_device_mapper_rootfs`: dmsetup staging into the jail, dmsetup removal at teardown) is not ported; a rootfs path that is not a regular file fails the boot with the Python "Not a file or a block device" message (byte-identical since the increment 4 review batch: the bare path, no OS error appended, matching `Path.is_file()`'s silent False for missing paths). The spec path only ever stages squashfs files today (the agent resolves program rootfs to a file), so the branch is unreachable from the current client. | Porting live dmsetup semantics with no reachable consumer would be untestable dead weight; revisit if a spec producer ever names a device-mapper rootfs. | fix-in-rust (when reachable) |
| 41 | RunProgramCode racing a failing boot: after CreateVm's failure path forgets the execution, the Python `becomes_ready()` await never resolves (the event is gone with the execution), so the RPC hangs until the client deadline. The Rust `vm_lock` stand-in (entry 21) acquires once the boot finished or failed; a failed boot removed the entry, so the call answers NOT_FOUND immediately. | A prompt NOT_FOUND is strictly better than a silent hang; the client retries through the create path either way. | keep |
| 42 | Jail staging edges of the ephemeral launcher, matched bug-for-bug where reachable: (a) a duplicate staged-file basename silently aliases the earlier file's content in BOTH daemons (Python enable_kernel/enable_file_rootfs catch FileExistsError, and enable_drive's `except OSError` clause precedes its dead `except FileExistsError` clause and swallows EEXIST without copying), a data-integrity hazard when two spec disks share a basename; (b) the kernel EXDEV path errors in both (enable_kernel catches only FileExistsError) while rootfs/drives copy on EXDEV; (c) enable_drive's bare `except OSError` also swallows every OTHER staging error (permissions, ENOSPC) and boots toward a missing drive, where the Rust daemon fails the create with the staging error (fail-fast instead of a deferred obscure boot failure). | (a) should refuse the collision in both daemons together; (b) is parity; (c) diverges deliberately: silently dropping a requested drive is worse than failing the create. | (a) fix-in-both-later, (b) done, (c) keep |
| 43 | erase_volumes on an ephemeral program: Python's `VmExecution.erase_volumes` reads `self.resources.volumes`, which SpecProgramResources does not have, so ReinstallVm(wipe_volumes=true) crashes AttributeError->INTERNAL after the rootfs was already unlinked and DeleteVm(wipe=true) crashes after the delete already ran, extra disks untouched in both. The Rust daemon pins the same DISK outcome (reinstall-wipe removes the rootfs, extra disks are never touched, delete-wipe touches nothing) but answers success instead of reproducing the destructive-then-INTERNAL crash. | The Python behavior is a plain bug (a crash on a reachable path, after the destructive step); reproducing the INTERNAL would make the agent retry a half-done operation. Python should adopt the Rust outcome, then both should decide whether program extra disks are supposed to be wiped at all. | fix-in-python-later |
| 44 | Rust-only robustness bounds on the increment 4 surfaces: (a) concurrent StreamLogs follows are capped at 64 (each live follow pins one blocking-pool thread; unbounded, ~512 follows would exhaust tokio's blocking pool and wedge every lifecycle RPC), the 65th answers RESOURCE_EXHAUSTED with the InsufficientResources trailer where Python (one asyncio task per stream) accepts unboundedly; (b) float-seconds timeouts (RunProgramCode's untrusted `timeout_secs`, settings INIT_TIMEOUT) clamp to 100 years instead of feeding `Duration::from_secs_f64` values that panic, where Python's asyncio.wait_for accepts any float; (c) concurrent DownloadBackup streams are capped at 16 (each pins one blocking-pool thread reading the archive chunk by chunk), the excess answering RESOURCE_EXHAUSTED with the InsufficientResources trailer, where Python gates neither the downloads nor the log follows. | Blocking threads are a bounded resource the Python daemon does not consume; the clamp turns a crafted-input panic into Python's observable "never fires" semantics, and the download cap extends the log-follow cap to the other blocking-thread streaming surface. | keep |
| 45 | Mid-boot guest_channel_path visibility: Python's GetVm serves `fvm.vsock_path` as soon as the MicroVM object exists, so a concurrent GetVm DURING an ephemeral boot already shows the channel path (unusable until the guest is ready). The Rust entry records the channel facts only after the ready handshake succeeds, so mid-boot GetVm shows an empty guest_channel_path. | The path is dead until the ready signal anyway; advertising it early invites clients to dial a channel that cannot answer. | keep |
| 46 | Reattach-retry exhaustion is invisible over the wire in BOTH daemons: after the 5th failed pass the VM stays hidden (NOT_FOUND) with only a log line (`tracing::error!` here, logger.error in pool.py) recording why; no RPC reports "adoption failed, retries exhausted". | Shared observability gap: an operator diagnosing a vanished VM needs journalctl. A health/diagnostics surface should expose the failed-reattach set in both daemons. | fix-in-both-later |
| 47 | Backup metadata cosmetics (increment 5), grouped: (a) the `.tar.meta.json` sidecar serializes compact (serde_json) where Python `json.dumps` uses `", "`/`": "` separators, and (b) a corrupt sidecar yields an empty BackupInfo. Python's `_backup_info_from_tar` catches only `(tarfile.TarError, OSError)`, so a corrupt `.tar.meta.json` (valid file, invalid JSON) escapes as a JSONDecodeError -> INTERNAL there; the Rust `backup_metadata` treats ANY read/parse failure (including a corrupt meta.json) as empty metadata and still returns COMPLETE. Both read the archive members and the checksum sidecar the same way; the meta.json is a hint, not the record. | (a) extends entry 6: consumers parse the JSON, nobody diffs bytes, and a rolled-back Python daemon reads compact JSON fine. (b) a corrupt-sidecar edge where returning the archive (which is intact) is strictly more useful than a 500; the archive bytes are the source of truth. | keep |
| 48 | Shared wart, ported bug-for-bug: `create_qemu_disk_backup` names its intermediate compressed copy `{vm_hash}-{%Y%m%dT%H%M%SZ}.qcow2` with SECOND precision, so an `include_volumes` backup of two or more disks within the same wall-clock second writes them all to the SAME destination path (each qemu-img convert overwrites the last), and the archive then tars whichever file won last under every member name. Reachable only with include_volumes AND >=2 disks AND sub-second timing on a running QEMU VM (KVM-gated). The Rust daemon reproduces the collision (same second-precision intermediate name) rather than de-duplicating. | Parity first: fixing one side's intermediate naming would diverge conformance. Both should switch to a per-disk unique name (the backup id's microsecond stamp, or the member name) together. | fix-in-both-later |
| 49 | Confidential surface boundary (increment 6), grouped. (a) InitializeConfidential is ported 1:1 with NO hardware dependency: it writes `vm_session.b64`/`vm_godh.b64` under CONFIDENTIAL_SESSION_DIRECTORY/<vm_id> and enable_and_starts the controller unit, exactly as Python. (b) GetMeasurement and InjectSecret are QMP passthrough to a running confidential QEMU (`query-sev`, `query-sev-launch-measure`, `sev-inject-launch-secret`, `cont`): the QMP protocol client (greeting + qmp_capabilities handshake, event-skipping command loop) is ported and unit-tested against a fake QMP socket, but the SEV data path only answers on an SEV host, so those two are HARDWARE-GATED (Tier 2), like the Python path (the QemuVmClient constructor already fails "VM is not running (QMP socket missing)" without a live confidential VM, byte-identical here). measurement_bytes rides empty on both sides; tee_backend is always SEV (SEV-SNP is a distinct launch path not emitted in-process). `query_sev_info` defaults any missing SEV field to 0/empty where Python's `VmSevInfo(...)` KeyErrors on an incomplete `query-sev` reply, but within the Tier-2 boundary a real SEV host always populates every field, so the field-level leniency is unobservable. (c) Confidential CreateVm builds the QemuConfidentialVMConfiguration (the four SEV fields after gpus, confidential cloud-init with the LUKS growpart bootcmds and no guest agent) and stamps starting_at+started_at WITHOUT enabling the unit, so the VM reports awaiting_confidential_init until (a) runs, mirroring pool.create_vm_from_spec's `execution.start` for a confidential VM. Two ordering differences from Python, both fail-fast-before-side-effect (entry 26 spirit): the missing-firmware refusal (InvalidBackendError) fires in build_written_config BEFORE the tap is created, where Python builds the confidential config after create_tap; and a malformed SEV policy aborts INTERNAL before side effects. `parse_sev_policy` matches CPython's `int(policy, 0)` grammar exactly (0x/0o/0b prefixes, decimal with no leading zero except the value zero, single underscores only between digits or immediately after a base prefix; so `010`/`07`/`0755`/`1__0`/`_10`/`10_` are rejected and `0x1_0`/`0X1F`/`00` accepted, pinned by a cargo test against the CPython truth table). The ONE intentional narrowing: a syntactically valid but negative (`-5`) or `> u32::MAX` policy, which Python's signed arbitrary-precision `int` accepts, cannot fit the `sev_policy: u32` field and is rejected INTERNAL rather than silently truncated; such a policy is a small unsigned bitfield in practice and is not reachable from a real agent. The real SEV attestation stack is Phase 3 (aleph-cvm); nothing here invents confidential behavior beyond Python's. | The 1:1 Python port of the confidential surface: the non-hardware work is reproduced exactly and testable; the SEV-hardware work is ported code marked Tier 2, matching the existing hardware-gated boundary. | keep |
| 50 | Rust-only read timeouts on the QMP/QGA sockets (increment 5-6): the QGA fs-freeze/thaw carry Python's 30s `asyncio.wait_for` bound, and the QMP client applies a 30s read timeout Python's blocking `qmp` library does not, so a wedged monitor degrades instead of hanging a lifecycle RPC. Both connections additionally set a write timeout matching the read timeout (a peer that never drains its socket cannot block `write_all` forever, parking the blocking-pool thread) and cap a single response line at 8 MiB (a peer that streams bytes without ever sending a newline cannot grow the read buffer unbounded). | The blocking-thread robustness family (entry 44): a bounded read/write and a bounded line cannot pin a blocking-pool thread or its memory forever. Observable only against a hung or hostile QEMU monitor, where a bounded failure beats an unbounded hang. | keep |
| 51 | Restore serializes on the daemon vm_lock (entry 21) in addition to the per-VM backup disk lock (the Python `_backup_locks`, ported): RestoreBackup and RestoreFromImage take the vm_lock around the stop/restore/restart, where Python holds only its async backup lock and leans on the event loop to serialize against other mutations. The background StartBackup run takes only the shared disk lock (no vm_lock), matching Python. DeleteVm (the tracked path) now ALSO takes the per-VM backup disk lock around the world-entry removal and the wipe erase_volumes, and reaps the VM's backup registry entry (jobs, the in-flight task slot, the disk lock) afterwards, so a DeleteVm cannot race a StartBackup's qemu-img read of the same disks (a corrupt member or a spurious FAILED job) and a completed/failed run does not linger in ListBackups for a deleted VM. Lock order: vm_lock, then the backup disk lock, then (inside stop/start) net_lock. | The same "blocking threads have no cooperative scheduling" rationale as entry 21: serializing restore and delete against a concurrent backup of the same VM is strictly safer, and the agent serializes per VM anyway. Python leaks the registry entry on delete (a dead VM's FAILED job stays in `_backup_jobs`), which the Rust reap fixes. | keep |
| 52 | RestoreBackup and RestoreFromImage drop Python's `is_persistent`/`support_systemd` guard (`local.py`: restore refuses a non-persistent or non-systemd execution). The Rust restore paths resolve the rootfs via `qemu_rootfs_path`, which already refuses a program (ephemeral, InvalidBackend), and CreateVm refuses a non-persistent QEMU VM up front (entry 26), so every restorable VM is a persistent systemd QEMU instance and the dropped guard is unreachable. | The guard cannot fire given the create-side refusals; porting a dead precondition would be untestable weight. | keep |
| 53 | Backup cleanup robustness (increment 5), grouped. (a) `cleanup_expired_backups` sweeps stale intermediate `.qcow2` files (the per-disk compressed copies, and `.restore.qcow2` staging files) older than the TTL, where Python's `cleanup_expired_backups` globs only `*.tar` and leaves an intermediate behind when the daemon is killed mid-backup or mid-restore. The happy path unlinks the intermediates in both daemons; this only reclaims a killed-run leak, and the backup directory holds nothing else named `.qcow2`. (b) `quiesce_guest` best-effort fs-freeze is a no-op when the guest agent is not installed (the QGA socket is absent, so `try_fsfreeze` logs and proceeds unfrozen) in BOTH daemons: a backup of a running VM without qemu-guest-agent is crash-consistent, not quiesced, on either side, and the freeze/thaw race window is identical. | (a) a Rust-only leak-reclaim in the same spirit as the blocking-thread bounds (entry 44), harmless to a rolled-back Python daemon which simply ignores the swept transients; (b) a shared, pre-existing property recorded so nobody mistakes the fs-freeze for a hard guarantee. | keep |
| 54 | Controller port scope note (Phase 3 increment A1, rust/crates/supervisor-controller, not the daemon): journal streaming is FAIL-OPEN. If journald is unavailable (no `/run/systemd/journal/stdout`, a dev box or container), the Rust controller logs a WARN and falls back to the controller's inherited stdout/stderr, then still launches the VM; Python's `journal.stream` raises and aborts the launch (the QEMU process is never spawned). | Deliberate for dev/container runs where a VM must still boot without systemd-journald; production nodes always have journald, so the fallback never fires there. The VM's console output just lands on the inherited streams instead of the `vm-{hash}-stdout`/`-stderr` tags. | keep |
| 55 | Controller port (increment A1): the controller exits 0 even when qemu exits non-zero or is signal-killed, matching Python's `__main__.py`, which returns after `process.wait()` regardless of `process.returncode` (no `sys.exit(returncode)`). This is a latent Python wart: under a systemd unit with `Restart=on-failure`, a crashed VM does NOT auto-restart because the controller reported success. Ported bug-for-bug so the two controllers behave identically; flagged for a future Python-validated fix (switch both to propagate the qemu exit status) rather than a unilateral Rust divergence. | fix-in-python-later |
| 56 | Controller port (increment A1): the Rust controller does NOT accept Python's `-i/--initialize-network-settings` flag. In the Python controller that flag runs `network.setup()` inline; in the agent/supervisor split, network setup is the daemon's responsibility and the production ExecStart never passes `-i`. The controller only ever WAITS for the supervisor-created `vmtap{vm_id}` (the else-branch of Python's `main`), never creates it. | Out of A1 scope: the flag drives daemon-side network setup the controller no longer owns. Accepting it would invite a controller to duplicate the daemon's job. | keep |
| 57 | Controller port (increment A1), cosmetic, grouped: the controller's OWN log line format differs from Python's `logging.basicConfig` format (`%(asctime)s | %(levelname)s | %(message)s`), and a signal-killed qemu is logged as exit `-1` (Rust `status.code()` is `None` for a signal death, defaulted to `-1`) where Python logs the real negative returncode (e.g. `-9`). Both still exit 0 (entry 55). | No consumer parses the controller's self-log; matching the exact format and the precise signal-derived returncode would cost real complexity for zero behavioral gain (the entry-33 family, controller edition). | keep |
| 58 | Controller port (increment A2, confidential SEV/SEV-ES): the AMD-SEV-platform pre-launch check fails closed on the SEV feature bit IN ADDITION to Python's condition. `SevHostInfo::read` reads CPUID leaf `0x8000001F` and returns `None` (which the launch path turns into the same "Not running on an AMD SEV platform?" refusal Python raises) when the extended leaf is unsupported OR the SEV feature bit (EAX bit 1) is clear. Python's `secure_encryption_info()` returns `None` only when the raw `cpuid(0x8000001F)` call raises (i.e. the leaf is above the host max extended leaf); it never inspects `has_sev`. On a non-AMD/Intel host the leaf `0x8000001F` is unsupported, so Python ALSO raises/returns `None` and both sides refuse identically: that is NOT a divergence. The genuine (narrow) window is an AMD part that implements leaf `0x8000001F` but reports `has_sev=0` (e.g. an SME-capable-but-not-SEV AMD SoC): there Python reads a NONZERO `c_bit_position` and `start()` proceeds to launch a bogus `-object sev-guest` VM, whereas Rust refuses. | Fail-closed: a confidential VM must never launch on an AMD part that exposes the encrypted-memory leaf but does not actually support SEV. The values are read identically on a real SEV host, so the only behavioral difference is refusing on hardware that could not run the VM anyway. | keep |
| 59 | Packaging cutover (increment A3), operational note (not a behavioral divergence): `ALEPH_VM_SUPERVISOR_IMPL` in `/etc/aleph-vm/supervisor.env` is a SINGLE source of truth that switches BOTH the supervisor daemon (via `supervisor-launcher`) AND the per-VM controller (via the new `controller-launcher`). `aleph-vm-controller@.service` now `EnvironmentFile=`s the same file and execs `controller-launcher`, which dispatches rust -> `/opt/aleph-vm/bin/aleph-vm-controller`, python/unknown -> `/usr/bin/python3 -m aleph.vm.supervisor.controllers` (unknown falls back to python, never bricks the node). A node can therefore never run the Rust daemon with the Python controller or vice versa. The launcher script is now shellcheck-linted in CI (the shellcheck step matches `*-launcher`, not only `*.sh`) AND covered by a dispatch test (`tests/test_controller_launcher.py`) that EXECS the real script and asserts all four branches (rust, python, unset -> python, bogus -> python) select the right target and forward the `--config` argument through `"$@"`; the test overrides the target paths via `ALEPH_VM_CONTROLLER_BIN` / `ALEPH_VM_CONTROLLER_PYTHON`, a seam whose defaults are byte-identical to the production paths. Distinct from that, the real-KVM integration harness (`tests/integration/conftest.py`) does NOT exec `controller-launcher`: it writes a systemd drop-in whose `ExecStart` points directly at the selected controller (source-tree exec root + PYTHONPATH shim, not `/opt/aleph-vm`), choosing the Rust binary under impl=rust and the python module under impl=python. The launcher's dispatch is therefore MIRRORED, not exercised, by the integration leg, which is fine because the script itself is now unit-tested by the dispatch test above. Reattach scope: flipping the impl and restarting the daemon is safe for STOPPED VMs and NEW launches; only same-impl daemon-restart reattach of a RUNNING VM is exercised by CI. The cross-impl reattach of a running VM (e.g. a python daemon reattaching over QMP to a QEMU that a Rust controller launched) is NOT exercised, so a node with running VMs should be cycled before flipping the flag. | Deliberate: the test path cannot use the packaged `/opt/aleph-vm` layout the launcher hardcodes, so it mirrors the launcher's selection; the launcher itself is covered directly by the exec'ing dispatch test. `assert_controller_matches_impl` fails the boot and daemon-restart tests loudly if the wrong controller ever runs (it checks that a live controller of the right kind is present by config path, after the boot-banner wait; it does not assert the controller -> qemu ancestry), so a launcher/drop-in drift cannot pass green. | keep |
| 60 | Packaging cutover (increment A3), env-injection note (not a behavioral divergence): adding `EnvironmentFile=/etc/aleph-vm/supervisor.env` to `aleph-vm-controller@.service` (required so the SAME `ALEPH_VM_SUPERVISOR_IMPL` flag reaches `controller-launcher`) now injects the WHOLE `supervisor.env` into the controller process, where before it carried only `PYTHONPATH`. This is intentional (single source of truth for the impl flag: one file, one flag switches daemon and controller together) and benign today: the controller reads its runtime settings from the `--config` JSON, not from the global settings singleton (`aleph.vm.conf`), so the extra daemon env vars are inert. Latent risk recorded: a FUTURE controller code path that read the global settings singleton would start picking up the daemon's env (e.g. `ALEPH_VM_SUPERVISOR_GRPC_SOCKET`, `ALEPH_VM_DOMAIN_NAME`, `ALEPH_VM_PAYMENT_RECEIVER_ADDRESS`), so any such addition must be reviewed against what `supervisor.env` carries. | Deliberate: the impl flag must be single-source-of-truth across daemon and controller, which requires the shared EnvironmentFile; the injection is harmless while the controller stays config-JSON driven. | keep |
| 61 | aleph-tee SEV-SNP verification (Phase 3 increment B0, rust/crates/aleph-tee) is HARDENED beyond its donor. This entry diverges from the aleph-cvm Rust donor (not the Python daemon, which has no SEV-SNP verifier). (a) The ARK is now PINNED to AMD's genuine root: `verify_cert_chain` compares the fetched/cached chain ARK's SubjectPublicKeyInfo against AMD's real ARK for the product, sourced from the `sev` crate's vendored roots (`sev::certs::snp::builtin::{milan,genoa,turin}`). The donor validated the chain only internally (ARK self-signed, ASK<-ARK, VCEK<-ASK) and tied it to AMD by a forgeable string check alone (CN starts "ARK-", O == "Advanced Micro Devices", self-issued); a fabricated self-signed ARK carrying those strings plus an attacker ASK/VCEK and a report signed by the attacker VCEK passed. The CN/O check is kept as secondary, non-security metadata. A wrong pin fails CLOSED (rejects a genuine report), the safe direction. (b) Chain validity (notBefore/notAfter) is now checked; an expired ARK/ASK/VCEK is rejected (donor did not check dates). basicConstraints CA:TRUE / key-usage on the ASK is NOT yet checked (openssl 0.10 exposes no ergonomic accessor; deferred, see entry 62). (c) KDS response bodies are capped at 256 KiB (streamed) so a hostile/MITM body cannot OOM the verifier. (d) `product` is validated against an allowlist (`Milan`/`Genoa`/`Turin`) before being interpolated into the on-disk cache path or KDS URL, closing a path-traversal/URL-injection vector. (e) `der_object_length` long-form length parsing uses `checked_mul(256)+checked_add` and rejects lengths past the buffer, replacing a `checked_shl(8)` misuse whose overflow branch was dead and could panic on a crafted length. | Security-critical: the string-only ARK tie is forgeable and the whole chain's trust rests on the root, so the pin is the real defense. Recommend UPSTREAMING the pinning (and the cert-validity, body-cap, product-allowlist, and DER-length fixes) to aleph-cvm so the donor and this port do not drift and the donor stops accepting forged ARKs. | keep |
| 62 | aleph-tee `verify_ark_identity` reads the ARK CN/O with `String::from_utf8` of the raw ASN.1 name-entry octets, where the aleph-cvm donor used openssl's `X509NameEntryRef::data().as_utf8()`. `as_utf8()` is deprecated in our openssl (0.10.81) and trips `clippy -D warnings`, which the workspace enforces. AMD's CN ("ARK-{product}") and O ("Advanced Micro Devices") are ASCII, so `String::from_utf8` of the DisplayString/PrintableString octets is equivalent-or-stricter (it rejects non-UTF-8 rather than lossily transcoding) and offers no bypass: this is only the metadata check, with the real trust decision made by the root pin (entry 61). | keep |
| 63 | aleph-attest-agent (Phase 3 increment B2a, rust/crates/aleph-attest-agent) is a faithful port of the aleph-cvm in-guest attested-TLS sidecar with three deliberate adaptations. This entry diverges from the aleph-cvm Rust donor, not the Python daemon (which has no in-guest agent). (a) The `/confidential/inject-secret` one-shot state moved from the donor's process-global `static INJECTION_LOCK: Mutex<Option<()>>` plus the hard-coded `/tmp/secrets` const into an injectable `SecretStore { injected: Mutex<bool>, dir: PathBuf }` held as actix `web::Data`. The HTTP contract is byte-identical (same 400/409/500 bodies, same MAX_SECRETS=16 / MAX_KEY_LEN=64 / MAX_VALUE_SIZE=64KiB limits, same all-or-nothing validation, same mode-0600 writes, same `Zeroizing` of values, same "mark injected even on partial write" semantics, production still `/tmp/secrets` via `with_default_dir()`), but the state is now testable against a temp dir. This ADDS the inject-secret unit tests the donor never had (one-shot 409 on repeat, empty/too-many/oversized-key/invalid-key-chars/oversized-value limits, mode-0600, and "a validation failure does not consume the one-shot guard"). The donor's global-static design was untestable in a shared test binary (one success would poison the process-wide guard for every other test). (b) rustls provider reconciliation is STRICTER than the donor: the workspace pins `rustls` with `default-features=false, features=["std","tls12","logging","ring"]`, which unifies the whole tree (actix-web's `rustls-0_23` via tokio-rustls, and reqwest's `rustls-tls`) onto the ring provider and keeps aws-lc-rs entirely OUT of the dependency tree. The donor's lock carried BOTH ring and aws-lc-rs and only disambiguated at runtime by naming the ring provider; here aws-lc-rs never compiles at all. `tls.rs` still installs `rustls::crypto::ring::default_provider()` by name (no reliance on a process-default provider). This also removes the aws-lc-sys C build from the static-musl path (deliverable B). (c) The donor crate listed `hyper` and `hyper-util` deps that no source file uses; dropped here. All other modules (`attestation.rs`, `proxy.rs`, `tls.rs`, `main.rs`) are verbatim ports depending on THIS workspace's aleph-tee (the B0 crate). The report-generation path (`SevSnpBackend::get_report` -> `/dev/sev-guest`) is exercised only by the mock-backend tests; no test hits real hardware, and no mock report is invented. | Faithful port with a testability refactor that preserves the exact HTTP behavior, a provider pin that is strictly safer and leaner than the donor, and a dead-dependency cleanup. Recommend UPSTREAMING the SecretStore refactor and the ring-only pin to aleph-cvm so the donor gains the inject-secret tests and drops the unused aws-lc build. | keep |
| 64 | Measured-boot Nix flake (Phase 3 increment B2a, `nix/`) is a SCOPED backport of aleph-cvm's `nix/flake.nix`, diverging from the aleph-cvm Nix donor (not the Python daemon). (a) SCOPE: only the CORE measured-boot chain is ported: the measured OVMF (`ovmf.nix`, AmdSev variant with kernel-hashing, verbatim), the kernel (`kernel.nix`, verbatim), the initrd (`initrd.nix`) baking the aleph-attest-agent static-musl binary + `init.sh`, a minimal dm-verity-protected rootfs (`rootfs.nix`), and the precomputed `sev-snp-measure` launch measurement. EXCLUDED at initial B2a scope (design section 6 non-goals): `compose-rootfs.nix`, `compose-demo/`, the donor's `fib-service` demo app (and its OCI image, `compose-workload`, and all `compose-*` verity/measurement outputs), and the encrypted-rootfs (LUKS) mode. AMENDED 2026-08-19: `compose-rootfs.nix` is no longer excluded. It is ported as the `aleph.compose/1` workload-runtime flavor, a podman + podman-compose platform userland that boots a caller-supplied `docker-compose` stack from a separate measured workload volume (`nix/compose-rootfs.nix`, `nix/init-compose.sh`, and the `composeInitrd`/`composeRootfs`/`composeImage` flake outputs), reusing the same CRN launch path as `aleph.exec/1` unmodified; see `docs/plans/2026-08-19-compose-runtime-port-design.md` for the full contract. The donor's `compose-demo/` scaffold and its bundled OCI demo artifacts remain unported (this port carries no demo workload of its own; workloads are caller-supplied), and the encrypted-rootfs (LUKS) mode remains excluded. (b) The `fib-service` demo workload is replaced by a trivial placeholder: a static busybox httpd serving one page on `127.0.0.1:8080`, which is exactly the upstream the attest-agent reverse-proxies to (enough to boot the measured image and exercise the agent in front of a real workload). (c) `initrd.nix` drops `dm-crypt.ko` (LUKS-only) and keeps just the dm-verity modules (dax, dm-mod, dm-bufio, dm-verity). `init.sh` is stripped of the LUKS branch and the compose workload-volume branch; what remains is the donor's dm-verity rootfs path (verify + mount ro, or plain-mount fallback) plus the attest-agent launch, so the reviewer should diff `nix/init.sh` against the donor's dm-verity branch specifically (the network setup and the dm-verity setup are otherwise verbatim). (d) The flake builds the attest-agent from THIS repo's crate: `craneToolchain.buildPackage` with `src = ../rust`, `cargoExtraArgs = "-p aleph-attest-agent"`, static musl (`+crt-static`) and static openssl (openssl-sys via the sev crate). No protoc/`.proto` is needed because the agent's dependency closure does not touch the proto/tonic crates. (e) REPRODUCIBILITY HARDENING over the donor (design section 8's top risk: a non-reproducible image makes the precomputed measurement useless). The donor left both `mkfs.ext4` and `veritysetup format` nondeterministic (random hash seed, random verity salt), so its measurement was not reproducible. Here the rootfs is built deterministically (fixed UUID, fixed NON-zero `hash_seed` (an all-zero seed is treated as unset and regenerated randomly), `-O ^has_journal` since a read-only verity rootfs needs no journal and the journal is a nondeterminism source, `lazy_itable_init=0,lazy_journal_init=0`, and `SOURCE_DATE_EPOCH`), and `verity` uses a fixed `--salt` and `--uuid`. Both `rootfs` and `verity` pass `nix build --rebuild` (byte-identical on rebuild), so the dm-verity roothash and therefore the sev-snp measurement are byte-reproducible. (f) `measurementFor { vcpus, vcpuType }` stays an INTERNAL flake function (a function is not a valid flake `packages` output and would fail `nix flake check`); only the concrete `measurement` (2 vCPUs, EPYC-v4 / Genoa) is exposed, alongside `attest-agent`, `ovmf`, `kernel`, `initrd`, `rootfs`, `verity`, and an `image` convenience directory (default). (g) OPERATIONAL: the flake lives at `nix/` but references `../rust`, which escapes the flake directory, so it MUST be evaluated with the git repository as the flake root, e.g. `nix build 'git+file:///<repo>?dir=nix#attest-agent'`, NOT `nix build ./nix#...` (the latter copies only `nix/` to the store and cannot see `../rust`). `nix flake check --no-build` fails on an unavoidable import-from-derivation: `measurement`/`image` `builtins.readFile` the verity roothash to form the kernel cmdline, which forces building `rootfs`+`verity` during evaluation (inherent to measured boot, present in the donor too). `workspace.package.version = "0.1.0"` was added to `rust/Cargo.toml` to silence crane's placeholder-version warning and give the agent derivation a real name. | Scoped port of the measured-boot chain, with the donor's demo scaffold and LUKS excluded per the design non-goals (compose was ported separately once a real V-PROGRAM workload contract existed for it, see (a)), and a genuine reproducibility fix (the single most fragile point in SNP support) that the donor lacked. The precomputed measurement is a real, reproducible function of (OVMF + kernel + initrd + cmdline-with-roothash + vCPU count + CPU type); it is NOT faked. Recommend UPSTREAMING the determinism fixes to aleph-cvm. | keep |
| 65 | aleph-attest-agent attestation `report_data` construction (Phase 3 increment B2a review fix) DIVERGES from the aleph-cvm Rust donor's wire format (not the Python daemon). The donor placed the caller's raw nonce DIRECTLY into the 64-byte `report_data` for the fresh-attestation endpoint, sharing the same namespace as the key-binding report (which stored `SHA-384(public_key)`). An attacker could request `/.well-known/attestation?nonce=hex(SHA-384(their_own_key))` and receive a GENUINE, AMD-signed report whose `report_data` is byte-identical to a legitimate key-bound report for the attacker's key: an attested-key confusion enabling a full MITM. FIX: canonical, domain-separated, channel-bound constructors now live in the shared `aleph-tee` crate (`src/report_data.rs`, re-exported) so the constructing agent (B2a) and the verifying CLI (B2b) cannot drift. `key_bound_report_data(pk) = SHA-384(DOMAIN_KEY \|\| pk)` and `fresh_report_data(pk, nonce) = SHA-384(DOMAIN_FRESH \|\| pk \|\| nonce)` (each domain a distinct constant with a trailing `0x00` separator), written into the first 48 of 64 bytes, rest zero. The fresh scheme now BINDS the agent's real served TLS public key (channel binding: a relayed fresh report cannot be reused with a different key) and NEVER places the raw nonce verbatim, and the domain tags make the two schemes non-colliding. `AttestedTlsIdentity` gained `public_key_raw` and `AppState` carries `served_public_key_raw` so the endpoint binds the agent's actual key. Tests assert domain separation, channel binding, and the specific attack impossibility (for any attacker key K, `fresh_report_data(server_key, SHA-384(K)) != key_bound_report_data(K)`). This is a wire-format divergence from the donor: a donor verifier would not accept these reports and vice versa. Recommend UPSTREAMING to aleph-cvm so the donor closes the MITM hole and both sides converge on the domain-separated scheme. | Security-critical (BLOCKER): the donor's shared-namespace raw-nonce scheme is directly exploitable for attested-key confusion / MITM. Domain separation plus channel binding is the fix, and putting the canonical constructors in the shared crate prevents the agent and the future CLI from drifting. | keep |
| 66 | aleph-attest-agent `/confidential/inject-secret` endpoint is UNAUTHENTICATED (donor-inherited from the aleph-cvm Rust sidecar; not present in the Python daemon). The one-shot guard means the FIRST network-adjacent caller to reach the endpoint wins and writes the secrets (secret poisoning / injection DoS): any party that can open the attested TLS port before the legitimate owner can inject arbitrary secrets, or exhaust the one-shot to deny the owner. This review LEDGERS the gap and DEFERS the fix rather than inventing an auth scheme now: owner authentication (binding injection to the allocation/ownership model, e.g. an owner-signed request over the attested channel) is a later confidential-flow design step, tracked there. The B2a hardening that WAS applied is the storage path, not the caller identity: the secret directory is now created/validated as a real, agent-owned, 0700 directory (symlink and wrong-owner refused) and secret files are opened `O_EXCL \| O_NOFOLLOW` at mode 0600 (a planted `/tmp` symlink can no longer redirect the write or defeat the mode), and the check-and-write remains atomic under the `Mutex` (a new concurrency test asserts exactly one of two racing injections succeeds and the other gets 409). The testnets attest-only path does not exercise authenticated remote injection, so this does not block B2a. | Deliberate deferral: authenticated injection belongs to the ownership/allocation model (a later design increment), not to the sidecar in isolation; ledgered so it is not forgotten. The storage-side hardening (symlink/perm/atomicity) is orthogonal and landed now. | fix-later |
| 67 | aleph-attest-agent report-generation endpoints (`/.well-known/attestation` and the key-bound report built at startup) are UNAUTHENTICATED and UNRATE-LIMITED (donor-inherited). Each `/.well-known/attestation` request triggers a real hardware `GET_REPORT` ioctl to `/dev/sev-guest`, so an unauthenticated caller can drive a report-generation DoS against the PSP. Noted for completeness; rate-limiting (or a small cache / proof-of-work) is DEFERRED. Not exercised or blocked by the testnets attest-only path. | Deferred: low severity relative to the crypto and injection issues, and the right control (rate limit vs cache) depends on the deployment's expected verifier cadence; recorded so it is on the record. | fix-later |
| 68 | SEV-SNP host launch + create-path backend selection (Phase 3 increment B1). SNP is added ALONGSIDE the SEV/SEV-ES path (A2), which is untouched. There is NO Python oracle for SNP (the Python controller never emitted `-object sev-snp-guest`), so this increment has no parity-oracle counterpart in the Python daemon; the reference is the aleph-cvm Rust donor (`aleph-tee::sev_snp::qemu`, `aleph-compute-node::qemu::args`/`verity`/`vm::manager`) plus the B2a measured image. (a) MEASURED DIRECT-KERNEL boot: the controller's `build_snp_argv` emits `-bios {measured OVMF}`, `-kernel/-initrd/-append "{measured cmdline}"`, `-cpu EPYC-v4`, `-smp {vcpus}`, and the `sev-snp-guest` object with `kernel-hashes=on`, matching the EXACT inputs the B2a `sev-snp-measure` covered (OVMF + kernel + initrd + cmdline + vcpu count + EPYC-v4). It boots DIRECTLY (no `-S`) and injects NO launch secret (SNP secrets are injected at runtime over the attested TLS channel by the in-guest attest-agent), unlike the SEV/SEV-ES path which starts paused and waits for the owner session. (b) `cbitpos` / `reduced-phys-bits` are READ FROM THE HOST at launch (increment D1), NOT hardcoded: `run_snp` calls `SevHostInfo::read()` (CPUID leaf `0x8000001F`) and injects the result into `build_snp_argv`, exactly like the SEV/SEV-ES path (A2). This makes the supervisor architecture-agnostic (a dumb launcher, agnostic of the host EPYC generation) instead of tied to the B1-era hardcoded `cbitpos=51,reduced-phys-bits=1`. These are HOST hardware properties that configure memory encryption and are NOT SEV-SNP measurement inputs (they do not enter the launch digest), so reading them from the host does NOT affect the measurement; on the current EPYC target CPUID reports 51/1, which is what the B2a precomputed measurement's target host reports. `-cpu EPYC-v4` (which IS a measurement input, via the per-vCPU VMSA) stays FIXED so measurements remain host-independent (a fixed vcpu-type is the correct verifiable-program behavior). Both `snp_tee_fragment` and the aleph-tee generator `sev_snp_qemu_args` are now PARAMETERIZED on `(cbitpos, reduced_phys_bits)`; the conformance test asserts they agree byte-for-byte across memory/policy/ovmf/NUMA variations AND crosses two host pairs (51/1 and a non-51 47/2), proving neither implementation hardcodes 51. `aleph-tee` is a DEV-only dependency of the controller so the runtime binary does not pull its openssl/reqwest closure. (c) The measured kernel cmdline is DERIVED in the daemon, not carried over the wire: the proto is frozen and has no cmdline/roothash field, so `snp_config_slice` reads the dm-verity roothash from the `{rootfs}.roothash` sidecar and the hash tree from `{rootfs}.verity` (the convention the B2a Nix `image` emits and the donor's `ensure_verity` uses), then builds `console=ttyS0 root=/dev/mapper/verity-root ro roothash={roothash}` exactly as the donor's `build_kernel_cmdline`. The roothash is validated as bare hex before interpolation (no kernel-parameter injection); a missing/malformed sidecar, or an empty `kernel_path`/`initrd_path`, fails CLOSED (InvalidBackend) so a mismeasured SNP VM never boots. The hash tree is inserted as the FIRST host volume (`/dev/vdb`), before any agent extra disks, mirroring the donor's `vm::manager` insertion at index 1. (d) An SNP VM carries NO cloud-init drive (it boots the measured Nix image whose in-guest `init` handles networking via DHCP and provisioning); the measured cmdline deliberately omits `ip=` for measurement determinism. (e) The SNP controller-config fields (`sev_snp: true` marker + `kernel_path`/`initrd_path`/`kernel_cmdline`, reusing `ovmf_path`/`sev_policy`) are Rust-only JSON (skip-if-None, so plain and SEV/SEV-ES configs stay byte-identical to the pydantic writer). A rollback to the Python controller reading an SNP config would `extra="ignore"` those fields and misclassify it as a plain QEMU config, but this is unreachable: Python never creates or adopts SNP VMs. (f) `HostInfo.sev_snp_supported` is now populated from a real host check mirroring the Python `check_amd_sev_snp_supported` (`/sys/module/kvm_amd/parameters/sev_snp == "Y"`, and NOTE it does not also require `/dev/sev`, unlike the SEV/SEV-ES checks); `sev_supported`/`sev_es_supported`/`tdx_supported` still ride empty (out of B1 scope). `VmInfo.confidential_mode` reports `SEV_SNP` and `awaiting_confidential_init` is false for SNP (no session to await). | No Python oracle exists for SNP, so faithfulness is measured against the aleph-cvm donor and the B2a measurement inputs, not the Python daemon. The byte-parity oracle test and the derived-from-sidecar cmdline are what keep the LAUNCHED measurement equal to the B2a precomputed value; deviating on any of OVMF/kernel/initrd/append/EPYC-v4/vcpu-count would break attestation. cbitpos/reduced-phys-bits are now HOST-READ (D1) rather than hardcoded, which is SAFE because they are not measurement inputs (only `-cpu EPYC-v4` and the boot blobs are), and it makes the supervisor architecture-agnostic. Recommend UPSTREAMING the host-read to aleph-cvm, whose `sev_snp_qemu_args` generator still hardcodes cbitpos=51/reduced-phys-bits=1; keep the oracle test as the drift guard against `aleph-tee`. GPU passthrough is REJECTED for SNP specs (`snp_config_slice` returns InvalidBackend when `spec.gpus` is non-empty): `build_snp_argv` emits no passthrough devices (confidential GPU / NVIDIA CC is a later feature), so accepting GPUs would write them into the controller config and then silently drop them at launch; failing closed at create time surfaces the unsupported combination to the owner instead of handing back a VM missing its hardware. | keep |
| 69 | SEV-SNP launch measurement / vCPU-count coupling (Phase 3 increment B1 review). The SEV-SNP launch measurement is a FUNCTION OF the vCPU count: each vCPU contributes a VMSA page to the measured digest. B1 emits `-smp {vcpu_count}` straight from the spec (flexible, whatever the client requested), while B2a's Nix flake ships the precomputed measurement for the DEFAULT config only (2 vCPUs, EPYC-v4). The daemon does NOT (and should not) enforce a specific vCPU count: the CLIENT pins the expected measurement for the vCPU count it requested, and a mismatch fails LOUDLY at the attest-cli verifier (not silently in the daemon or controller). Concretely: the testnets V-SNP scenario MUST create the SNP VM with the vCPU count whose measurement it pins (2), and any future multi-vCPU support needs the corresponding measurement shipped/computed for that count. This is recorded so the `-smp {vcpu_count}` flexibility is understood as client-pinned, not host-enforced. | The measurement-to-vCPU coupling is inherent to SEV-SNP (VMSA-per-vCPU); pushing enforcement to the client's verifier (loud mismatch) is correct, and the default measurement covers the 2-vCPU testnets path. Ledgered so the coupling is explicit and multi-vCPU support does not silently mismeasure. | keep |
| 70 | SEV-SNP NIC MAC / reboot flags follow aleph-vm, not the aleph-cvm donor (Phase 3 increment B1 review). The SNP NIC carries NO `mac=` and the launch uses `--no-reboot`, matching aleph-vm's Python controller (the port spec, A1/A2), NOT the aleph-cvm donor (which sets an explicit `mac=` and the single-dash `-no-reboot`). This is DELIBERATE parity with aleph-vm rather than the donor. DHCP-reservation consideration: if SNP VMs later need a stable MAC for MAC-based DHCP reservations, that is a separate networking task; the attestation flow connects via the daemon-reported IP, not a MAC lookup, so no attestation path depends on a fixed MAC today. | Consistency with the aleph-vm networking model (the spec) beats matching the donor's incidental MAC choice; a stable MAC is a future networking concern, not an attestation dependency. | keep |
| 71 | aleph-attest-cli client-side verifier (Phase 3 increment B2b, `rust/crates/aleph-attest-cli`, ported from the aleph-cvm Rust donor) verifies the NEW domain-separated, channel-bound `report_data` scheme, DIVERGING from the donor's old scheme exactly as the B2a agent did (entry 65). This is the verifier counterpart that closes the loop with B2a. (a) KEY BINDING (Layer 2, at TLS handshake in `SnpCertVerifier::verify_server_cert`): the donor checked `report.report_data == SHA-384(server_pubkey) \|\| zeros`; this port checks `report.report_data[..48] == aleph_tee::report_data::key_bound_report_data(server_pubkey)[..48]` where `key_bound_report_data(pk) = SHA-384(DOMAIN_KEY \|\| pk)`. The canonical constructor is imported from the SHARED `aleph-tee` crate (NOT reimplemented), so the verifier and the B2a agent cannot drift. (b) SERVER PUBKEY EXTRACTION: the verifier reads `cert.tbs_certificate.subject_pki.subject_public_key.data` (the SubjectPublicKeyInfo BIT STRING content, i.e. the uncompressed EC point for the P-384 key). This is BYTE-IDENTICAL to what the agent bound: the agent uses `rcgen::KeyPair::public_key_raw()`, which returns `kp.public_key().as_ref()` (the same raw EC point). Confirmed by reading rcgen 0.14.8 `key_pair.rs`. This encoding equivalence is the SINGLE parity hinge: if it ever broke, real attestation would fail closed (rejects a genuine server), which is the safe direction. (c) FRESH ATTESTATION (Layer 3, `client::verify_fresh_binding`): the donor placed the raw nonce in `report_data` and compared `report_data[..32] == nonce`; this port compares `report.report_data[..48] == fresh_report_data(server_pubkey, nonce)[..48]` where `fresh_report_data(pk, nonce) = SHA-384(DOMAIN_FRESH \|\| pk \|\| nonce)`. The verifier now RECOVERS the served TLS pubkey captured during the handshake (`SnpCertVerifier::get_server_public_key`, a new field populated in `verify_server_cert`) and binds BOTH key and nonce, defeating the relay / attested-key-confusion attack. (d) MEASUREMENT PINNING and the full AMD KDS chain verify are wired unchanged from the donor: `--expected-measurement` aborts the handshake before app data if the report measurement mismatches, and after the handshake `aleph_tee::sev_snp::verify::verify_sev_snp_report` runs the full VCEK->ASK->ARK chain, which is ARK-PINNED (entry from B0) so the CLI inherits root pinning automatically. (e) TESTS: 10 hermetic unit tests (self-generated P-384 keys + synthetic certs carrying an `AttestationReport`; the handshake path never parses the raw `data` bytes so no real report is needed): key binding accepts the domain-bound report and rejects a wrong binding; measurement pinning accepts expected / rejects wrong; a cert without the extension is rejected; fresh binding accepts key+nonce and rejects a wrong key (relay) or wrong nonce (liveness); and TWO key-confusion tests prove the attack is rejected end-to-end at the verifier: a report whose `report_data` is the donor's old raw `SHA-384(attacker_key)` (equally, a donor-era fresh report for `nonce = SHA-384(attacker_key)`) does NOT satisfy the verifier's domain-separated key binding for the attacker key. The real-KDS/hardware chain path is NOT re-tested in this crate (it is covered by aleph-tee's own `#[ignore]`d KDS test), so all 10 CLI tests are hermetic and run in CI. This is a wire-format divergence from the donor identical in spirit to entry 65: a donor CLI would reject these reports and vice versa. Recommend UPSTREAMING the agent (65) and this verifier (71) TOGETHER so the donor closes the MITM hole on both sides at once. | Security-critical: the verifier MUST match the B2a agent's domain-separated scheme or attested TLS would never validate; sharing the canonical constructors via `aleph-tee` is what guarantees the match. The pubkey-encoding equivalence (rcgen `public_key_raw()` == cert BIT STRING content) is the parity hinge and fails CLOSED. SEV-SNP is complete after B2b. | keep |
| 72 | aleph-attest-cli verifier SECURITY-FIX BATCH (Phase 3 increment B2b review fix, closes two BLOCKERs the initial B2b port inherited verbatim from the aleph-cvm donor, which is EXPLOITABLE on both counts; approved for immediate divergence). Attestation was bypassable two independent ways. FIX 1 - TRUST IS NOW BLOB-DERIVED, NOT FROM UNSIGNED COPIES: the `aleph_tee::types::AttestationReport` carried, alongside `data` (the raw 1184-byte AMD-signed blob, the ONLY bytes AMD's signature covers), standalone JSON `report_data: [u8;64]` and `measurement: Vec<u8>` fields. Those copies are UNSIGNED: an attacker replays ANY genuine AMD-signed blob as `data` (which passes the chain), sets JSON `report_data = key_bound_report_data(attacker_key)` and JSON `measurement = expected`, and the donor verifier (and the initial B2b port) gated key binding + measurement pinning + fresh-nonce binding on those JSON copies, so all checks pass. FIX: the two standalone fields were REMOVED from `AttestationReport` entirely (it now carries only `tee_type` + `data`), making it structurally impossible to gate trust on an unsigned copy. Every trust check now derives report_data/measurement by PARSING the AMD-signed blob: `verify_sev_snp_report` returns a blob-derived `report_data` in `VerificationResult` (wiring in `sev_snp::report::extract_report_data`, previously dead code) next to the existing blob-derived `measurement`; `SnpCertVerifier::verify_server_cert` parses `data` and checks the domain-separated key binding + measurement pin against the blob's OWN report_data/measurement; the fresh path binds the nonce to `VerificationResult.report_data` (the AMD-verified blob), never a copy. The B2a agent + aleph-tee + x509 encode/decode were updated to match (the agent embeds only `data`; the fresh endpoint serializes only `data`); agent + aleph-tee suites re-run green. Tests prove the property against the residual surface: even when an attacker SMUGGLES an extra unsigned `report_data`/`measurement` field into the cert-extension JSON (serde ignores unknown fields), the verifier REJECTS when the signed blob binds a different key / carries a different measurement, and ACCEPTS purely on the blob regardless of the smuggled copy. FIX 2 - FULL AMD CHAIN VERIFIED BEFORE ANY SECRET LEAVES THE CLIENT: the donor (and initial B2b) `inject_secret` called reqwest `.send()` (putting the secret POST body on the wire) BEFORE `verify_sev_snp_report` (the KDS/ARK signature chain); the sync handshake gate only checked key-binding + measurement, NOT the chain, so a peer with a correct key-binding + measurement but a FORGED/unsigned blob received the secret before the chain check ran. FIX: architecture (A) AUTHORITATIVE GATE. `SnpCertVerifier::verify_server_cert` now performs the COMPLETE verification (parse blob, blob-derived key binding, measurement pin, AND the full AMD chain `verify_sev_snp_report`) and returns `Err` unless ALL pass; the async chain is driven from the sync callback via `tokio::task::block_in_place` + `Handle::current().block_on` (main.rs `#[tokio::main]` provides the multi-thread runtime). The TLS handshake therefore completes ONLY for a fully-verified TEE, so `.send()` for ALL flows (attested_request / fresh_attestation / inject_secret) transmits application data (including secrets) only after full verification; the redundant post-send verify was removed and callers reuse the gate's stashed `VerificationResult`. Chosen over (B) verify-before-send because (A) makes EVERY connection uniformly safe with no per-flow reordering and no risk of verifying report X on connection 1 then sending on an unverified connection 2. block_in_place reliability is proven by a test where the injected chain check performs genuinely-pending async work (timer + real TCP connect, exercising the IO driver as a KDS fetch would) from inside the verify callback and completes without deadlock, plus the end-to-end reqwest handshake tests below which invoke block_in_place in the real reqwest handshake path. TESTS (all hermetic, no hardware/KDS; the AMD chain is an injected deterministic stub so ordering/gate behaviour is exercised in CI; the real KDS path stays in aleph-tee's `#[ignore]`d test): a local tokio-rustls attested-TLS server records whether it received the secret body; `inject_secret` with a FAILING chain check transmits ZERO secret bytes and errors, while a PASSING chain check delivers the secret and parses the response (positive control). Mutation kills CONFIRMED by hand: neutering the `if !result.valid` gate to `if false` FAILS `rejects_when_chain_reports_invalid` (the M7 mutation); neutering the chain-`Err` propagation FAILS the ordering test `inject_secret_does_not_send_when_chain_fails`; neutering the blob-derived key-binding check FAILS `rejects_blob_bound_to_other_key` and `rejects_when_only_smuggled_json_binds_key`. The tautological donor key-confusion test (an `assert_ne` on constructors) was replaced by one that drives `verify_fresh_binding`. Recommend UPSTREAMING BOTH fixes to aleph-cvm together with entries 65/71: the donor is exploitable on both counts (trusts unsigned copies; sends secrets before the chain). | Security-critical (two BLOCKERs): the donor's design let a forged/replayed report pass every trust check (unsigned copies) AND receive secrets before the AMD signature was ever checked (send-before-verify). Removing the unsigned fields makes the bypass unrepresentable; the authoritative gate makes secret transmission conditional on the full chain. Both are deliberate, permanent divergences from the exploitable donor. | keep |
| 73 | aleph-attest-cli duplicate-secret-key handling (Phase 3 increment B2b review fix). The donor built the inject-secret body by collecting the CLI `--secret K=V` pairs straight into a map, so a repeated key (`--secret K=v1 --secret K=v2`) silently kept only the last value with no warning. For a secret-injection tool that is a footgun (the caller may believe both landed). `inject_secret_with_verifier` now inserts pair-by-pair and bails (`duplicate secret key "K": each key may be given only once`) on the first collision, before any network I/O, so a duplicated key fails closed rather than dropping a value. Also in this review batch (dependency hygiene, not a behavioral divergence): the nonce RNG moved off `rand = "0.9"` to a direct `getrandom` call on the `getrandom 0.2` already in the tree, which drops the crate's sole-consumer `rand 0.9.4`/`rand_core 0.9.5`/`rand_chacha 0.9.0`/`getrandom 0.3.4`/`wasip2`/`wit-bindgen` chain from the lock. | keep |
| 74 | NUMA topology reporting + supervisor-side pack-first placement + vCPU pinning (Phase 3 increment C1, `rust/crates/supervisor-daemon/src/numa.rs`, ported from the aleph-cvm donor `aleph-compute-node/src/numa.rs`). There is NO Python oracle: the Python daemon never reported a NUMA topology or pinned CPUs, so this is a Rust-only, donor-referenced feature added ALONGSIDE the faithful port, never replacing anything. (a) TOPOLOGY: `NumaTopology::detect()` parses `/sys/devices/system/node/*` (cpulist, per-node MemTotal, and the 2M/1G hugepage counts) into `HostInfo.numa_nodes` (`{index, cpu_count, memory_mib}`), which rode empty before C1; `from_sysfs(base)` makes it hermetically testable against fixture trees. A detection failure DEGRADES to an empty topology (CPU pinning disabled) rather than aborting the daemon, mirroring the other host-capability probes; per-node parsing is ALSO fault-tolerant (a node with a missing/unreadable/malformed cpulist or meminfo is SKIPPED with a warning, not fatal to the whole topology), and a bogus cpulist range is bounded (a range wider than 4096 CPUs is rejected/skipped rather than materialized, so a kernel-quirk "0-4294967295" cannot OOM the daemon). The 2M hugepage count (like the 1G count) degrades to 0 on a read/parse failure - deliberate, since a node may expose no hugepage sysfs at all. PLACEMENT and PINNING activate ONLY when the host has MORE than one NUMA node (`is_placement_active` = `nodes.len() > 1`): every CONFIG_NUMA kernel exposes a `node0` even on a single socket, so a single-node host is treated exactly like a non-NUMA host - no placement, no drop-in, no daemon-reload, no reservation, `VmInfo.numa_node` stays None - while its one node is STILL reported in `HostInfo.numa_nodes` (reporting is independent of the >1-node gate). An empty topology is likewise inert. So every pre-C1 test and any single-node or non-NUMA host behaves exactly as before. (b) PLACEMENT LEDGER: `NumaAllocator` is an in-memory, supervisor-side pack-first allocator tracking ONLY per-node vCPU counts (node 0 filled before node 1, ...); it lives in `DaemonState.numa_ledger` behind a `std::sync::Mutex`, mutated under the existing per-create `creation_lock`. Decision 4 policy: a spec `numa_node` is HONORED (validated to exist, else a clean error), otherwise pack-first picks the node. The effective node is recorded on the `VmEntry` (new `numa_node: Option<u32>` field) and reported in `VmInfo.numa_node` (was always None). The donor's 2M/1G hugepage ALLOCATION POOLS are DEFERRED to C2 (only the `NumaNode` hugepage FIELDS are kept, for reporting and that follow-up); C1 does not touch the QEMU argv (CPU pinning is a systemd unit property, not a QEMU flag). (c) PINNING MECHANISM = systemd DROP-IN, not D-Bus SetUnitProperties: the chosen node's cpuset is written to `{SYSTEMD_UNIT_DIR}/aleph-vm-controller@{hash}.service.d/numa.conf` as `[Service]\nAllowedCPUs={cpuset}` before the controller starts, then a `Manager.Reload()` (new `UnitStateSource::reload()` trait method, default no-op for the in-memory fakes, D-Bus `Reload` on `ZbusUnitStates`) makes an already-loaded unit pick it up. Chosen over `SetUnitProperties` because the file is (i) hermetically testable via a temp `SYSTEMD_UNIT_DIR` (new setting, default `/etc/systemd/system`), (ii) durable across a daemon restart, and (iii) directly readable back for reconcile. The drop-in is written even for SEV/SEV-ES VMs that start later at `InitializeConfidential` (it persists on disk and applies at that start) and is removed on delete alongside the other teardown (tracked-delete releases the ledger by `entry.numa_node` + `config.vcpu_count`; the failed-adoption untracked-delete releases by mapping the on-disk drop-in cpuset back to a node). A create failure after allocation unwinds the reservation and drop-in. (d) INVALID-NODE mapping: a requested `numa_node` that does not exist maps to `RpcError::InvalidBackend` (gRPC `INVALID_ARGUMENT`, the correct code for a bad client argument; the `INVALID_BACKEND` trailer code is the closest existing `ErrorCode` since the frozen proto has no dedicated NUMA/argument code); capacity exhaustion maps to `InsufficientResources` (`RESOURCE_EXHAUSTED`). The proto was NOT changed (`NumaNode`, `VmSpec.numa_node`, `VmInfo.numa_node`, `HostInfo.numa_nodes` already existed). | No Python oracle exists for NUMA; faithfulness is measured against the aleph-cvm donor. The empty-topology-is-inert gate keeps parity for every non-NUMA host and pre-C1 test. The drop-in-file mechanism (vs D-Bus SetUnitProperties) is what makes pinning testable, restart-durable, and reconcilable. Recommend nothing to upstream (this is the port direction). | keep |
| 75 | NUMA ledger reconcile across daemon restarts treats UNKNOWN placement as UNPINNED, never node 0 (Phase 3 increment C1, design section 8 risk). The supervisor-side placement ledger is in-memory, so it must be rebuilt at boot from each adopted VM's EFFECTIVE placement. `reconcile_numa_ledger` reads each adopted-running controller's `AllowedCPUs` drop-in: a cpuset that maps EXACTLY to a node's CPU set re-registers that VM's vCPUs in the ledger (so pack-first stays correct after a restart) and sets its `VmInfo.numa_node`; a VM with NO drop-in (a pre-NUMA adoption, or one whose cpuset matches no node) is left UNPINNED and counts against NO node. This is the deliberate safe direction: silently attributing a pre-NUMA VM to node 0 would over-count node 0 and mis-pack every subsequent create. The same reconstruction runs when re-adopting a live-but-untracked controller (`readopt_live_controller`). NOTE the reconstruction runs as a DEDICATED `reconcile_numa_ledger` called UNCONDITIONALLY at boot from `main.rs`, NOT folded into `reconcile_boot` (which returns early when `ALLOW_VM_NETWORKING` is off): the ledger must be consistent even on a host with VM networking disabled, so it cannot ride the networking-gated path. `reconcile_numa_ledger` is itself inert when the topology is empty. | Adopted VMs predate NUMA support and carry no recorded placement; treating unknown as unpinned (not node 0) is the only choice that keeps pack-first correct. Splitting it out of the networking-gated `reconcile_boot` is required so it runs regardless of `ALLOW_VM_NETWORKING`. Covered by hermetic tests (a drop-in-bearing adopted VM re-registers, a drop-in-less one stays unpinned and uncounted). | keep |
| 76 | NUMA memory binding in the QEMU argv (Phase 3 increment C2). When the supervisor places a VM on a NUMA node (a >1-node host; `numa_node` set), the controller now binds that VM's guest RAM to the node with `-object memory-backend-memfd,id=pcram,size={mem}M,share=on,host-nodes={node},policy=bind` wired via `memory-backend=pcram` on the EFFECTIVE (last) `-machine` line. This is a NEW aleph-vm feature with NO Python oracle: the Python controller never emitted a memory backend, and the aleph-cvm donor binds memory ONLY for confidential VMs (its `qemu/args.rs` has no non-confidential memory backend). It applies to all three launch paths: (a) PLAIN (`build_argv`) - the `memory-backend=pcram` suffix lands on the no-GPU migration-pin `pc-i440fx-6.2` line (QEMU takes the last `-machine`) or on the sole `q35` line in GPU mode; (b) SEV/SEV-ES (`build_confidential_argv`) - the suffix lands on the `q35,confidential-guest-support=sev0` line; (c) SEV-SNP (`snp_tee_fragment`) - the existing B1 `ram1` memfd gains the `,host-nodes={node},policy=bind` suffix, kept byte-identical to the aleph-tee generator oracle (the generator already carried the logic via `VmConfig.numa_node`; the controller's fragment is now driven with the same values, and the oracle test crosses memory/policy with the numa/hugepage combinations). CRITICAL PARITY: the memory backend is emitted ONLY when `numa_node` is present. A single-node / no-NUMA host never sets `numa_node` (placement is inert per divergence 74), so its argv is BYTE-IDENTICAL to pre-C2 on every path (proven by the existing Python-generated conformance fixtures, which carry no `numa_node`, plus explicit with/without-node controller tests). The daemon carries `numa_node` (and `hugepage_size`) into the written controller config via new `skip_serializing_if=None` fields, so plain/SEV/no-NUMA/SNP configs stay byte-identical to the pydantic writer; the fields are injected AFTER `place_vm_numa` runs (it runs after `build_written_config`), before the config is written to disk. | No Python oracle exists (Python never bound VM memory to a NUMA node); faithfulness is against the aleph-cvm donor pattern extended to the non-confidential path, gated on active placement so no non-NUMA host changes. Recommend nothing to upstream (port direction). | keep |
| 77 | NUMA hugepage backing is OPT-IN behind `ALEPH_VM_NUMA_HUGEPAGES` (default OFF), ported from the aleph-cvm donor `aleph-compute-node/src/hugepages.rs` into `supervisor-daemon/src/hugepages.rs` (Phase 3 increment C2). Reserving hugepages writes host-wide `nr_hugepages` sysfs files (a real host memory reservation), so it never runs unless an operator enables it. OFF (default): no reservation at boot, no `hugetlb` in the memory backend - C2 delivers only the regular-page NUMA binding (divergence 76). ON (and a >1-node host): at boot `reserve_2m_hugepages` reserves 2M hugepages per node via `compute_2m_budget` (honoring any existing 1G reservation) with a per-node `headroom` floor (`ALEPH_VM_NUMA_HUGEPAGES_HEADROOM_MB`, default `DEFAULT_HEADROOM_MB` = 8192; an optional global cap `ALEPH_VM_NUMA_HUGEPAGES_LIMIT_MB` bounds the total reservation) so the host is never starved; the `NumaAllocator` then picks 1G (for 1G-aligned memory with free 1G pages) or 2M pages for a VM and the memory backend gains `hugetlb=on,hugetlbsize={1G|2M}`. FAIL-SAFE throughout: (a) a per-node boot reservation write failure is logged and SKIPPED (that node falls back to regular pages), never aborting boot; (b) the allocator selects a page size ONLY when the chosen node has hugepage capacity, else falls back to regular pages (`hugepage_size=None`) - it NEVER fails placement or downgrades WHICH node is picked, so node selection stays byte-identical to C1's pure vCPU pack-first (the hugepage pools are a separate accounting layered on top). Divergence from the donor: aleph-cvm couples hugepage capacity INTO node selection (bailing when no node has both CPU and hugepage room); here node selection is CPU-only (unchanged from C1) and hugepages are a best-effort overlay, because aleph-vm hugepages are an optimization (not a pvalidate correctness requirement as they are for aleph-cvm confidential VMs). Hugepage-pool accounting is exact across both the VM lifecycle and a restart: an in-session delete (and a failed-create unwind) returns the VM's reserved pages to the node's pool via `NumaAllocator::release(node, vcpus, memory_mb, hugepage_size)` (so the pool does not leak monotonically and silently downgrade every later VM to regular pages), and at reconcile each adopted VM is re-registered by vCPU AND its recorded hugepage usage, recomputed from its written config's `hugepage_size` + `mem_size_mb` via `register(node, vcpus, memory_mb, hugepage_size)` (so the pool is not reset to 0 across a restart, which would let the same pages be handed out twice and over-commit the host). A stop keeps both the vCPU and the hugepage reservation (start never re-places); only delete/failed-create release them. Boot latency (operator note): when enabled, `reserve_2m_hugepages` writes `nr_hugepages` SYNCHRONOUSLY at daemon boot, BEFORE the gRPC socket binds; on a fragmented host the kernel's page compaction to satisfy the reservation can add measurable startup latency. This is opt-in and boot-only (never on the per-VM create path), so operators enabling `ALEPH_VM_NUMA_HUGEPAGES` should expect a slower first-start on a fragmented host. Not moved to async for now. Unit-tested: `compute_2m_budget` (headroom/1G/cap), page-size selection (1G->2M->regular), the boot-reservation fail-safe (a node whose write fails stays at regular pages), the pool round-trip (allocate/release returns pages, no leak) and adoption re-registration (pool recomputed from the written config, not reset). No real multi-node / hugepage hardware in CI (temp-sysfs fixtures). | No Python oracle (Python never reserved hugepages); reference is the aleph-cvm donor, made opt-in and fail-safe because writing `nr_hugepages` is a real host reservation aleph-vm must not do by default. The CPU-only node selection keeps C1 placement and all its tests unchanged. Recommend nothing to upstream. | keep |
| 78 | SEV-SNP measured VMs get their IPv4 via a per-tap DHCP server (dnsmasq), where plain and SEV/SEV-ES VMs keep the cloud-init STATIC network config (`controllers/qemu/cloudinit.py create_network_file`) unchanged (Phase 3 increment D2, `supervisor-daemon/src/dhcp.rs`). WHY: the SNP measured image (nix/) deliberately omits `ip=` from its kernel cmdline so the launch measurement is host-independent (the daemon derives `console=ttyS0 root=/dev/mapper/verity-root ro roothash=...` with NO `ip=`, and that MUST stay that way), so its guest init (nix/init.sh) falls back to `udhcpc`. With no DHCP server on the tap the SNP guest never obtains its allocated address and is unreachable at `VmInfo.ipv4.address`, so attestation cannot connect. The tap already carries the gateway IPv4 (the host-side `host_ipv4_cidr` that `tap::create_tap` adds), so a DHCP server can bind and route. MECHANISM: when the daemon creates an SNP VM (gated strictly on `snp`; the SNP branch of `create_vm_inner`, right after `nft_setup_vm`), it stands up a dnsmasq as a transient systemd unit `aleph-vm-dhcp-{vm_hash}.service` via `systemd-run --unit=... --collect --property=Type=exec -- dnsmasq --keep-in-foreground --user=root --port=0 --interface=vmtap{N} --bind-interfaces --except-interface=lo --no-resolv --no-hosts --dhcp-authoritative --dhcp-leasefile={EXECUTION_ROOT}/dhcp/{vm_hash}.leases --dhcp-range={guest_ip},{guest_ip},{netmask},1h --dhcp-option=3,{gateway} --dhcp-option=6,{nameservers}`. `--property=Type=exec` is load-bearing (D2 review fix): under the default `Type=simple`, `systemd-run` returns success the instant the unit forks, so a missing/unrunnable dnsmasq would report a started job while the guest silently gets no DHCP; `Type=exec` holds the start job until dnsmasq has `execve`d so an exec failure fails the job and `start()` returns `Err`. `--user=root` suppresses dnsmasq's default setuid to the `dnsmasq`/`nobody` user (which need not exist on the host; the transient unit already runs as root). A `/`-less or non-numeric tap network CIDR now fails CLOSED in `DhcpConfig::for_snp` (returns `Err`) rather than silently yielding an unroutable `/32`. A SINGLE-address `--dhcp-range` (low == high == the allocated `VmInfo.ipv4.address`) is how the guest deterministically gets EXACTLY its allocated IP: the SNP NIC has no fixed MAC (ledger entry 70), so the donor's MAC-keyed reservation (`aleph-compute-node/src/network/tap.rs write_dhcp_reservation` + shared `--dhcp-hostsdir`) does not apply, and the single-IP range replaces it. `--port=0` disables the DNS server so nothing binds :53 and per-tap instances never collide; option 3 is the host tap gateway and option 6 the daemon's resolved nameservers (`obtain_dns_ips`, the same set cloud-init would inject). TEARDOWN is idempotent and SNP-gated, alongside the tap/nft teardown: `stop_vm_execution` (covering StopVm AND `delete_tracked_vm`), the failed-create cleanup, AND the `discard_failed_reattach` delete path (D2 review fix: a still-live SNP VM whose adoption failed previously leaked `aleph-vm-dhcp-{vm_hash}.service`; the discard path now parses the on-disk config and, when `snp().is_some()`, calls `dhcp.stop`) all call `dhcp.stop(vm_hash, lease_file)` -> `systemctl stop {unit}` (a missing unit is a success, like the tap-delete tolerance) AND best-effort `remove_file({EXECUTION_ROOT}/dhcp/{vm_hash}.leases)` so the per-VM lease database does not leak on persistent storage (the `DhcpBackend::stop` signature now takes the lease path). RESTART (StopVm -> StartVm of a persistent SNP VM, and the first start after a host reboot, where transient units are gone): `start_vm_execution` recreates the DHCP server alongside the tap/NDP/nft recreation when the entry is SNP, since stop tore it down with them; without this the rebooting measured guest (no `ip=` on its cmdline) could never lease its IP and attestation would be unreachable. `DhcpBackend::start` replaces a leftover unit (best-effort stop first), so a partial stop cannot fail the restart. Create keys DHCP startup on the request `snp` predicate while teardown keys on `config.snp().is_some()`; a `debug_assert_eq!` in create documents and guards that the two stay in agreement. GUEST SIDE: the measured Nix image's `nix/init.sh` runs `udhcpc -s /bin/udhcpc.script`, and `nix/udhcpc.script` applies the lease busybox-udhcpc does not apply itself (converts the dotted `$subnet` to a prefix with pure shell arithmetic, `ip addr add {ip}/{prefix}`, brings the link up, adds the default route via `$router`, and writes `/etc/resolv.conf` from `$dns`); without it the guest would lease the address from the per-tap dnsmasq but never bring up its IP, leaving the attest-agent unreachable. Deb dependency added: `dnsmasq-base` (the binary without the auto-started system dnsmasq service, as libvirt uses). The kernel/systemd edge is behind a `DhcpBackend` trait seam (like `TapBackend`); cargo tests assert the derived dnsmasq/systemd-run argv (incl. `--property=Type=exec`, `--user=root`), the single-address range, the option 3/6 derivation, the netmask-from-prefix, the CIDR-without-prefix rejection, the `is_absent_unit_error` stderr classification, the lease-file removal on stop, and start-on-SNP-create / no-start-on-plain-create / no-start-on-SEV-ES / teardown-on-stop/delete / teardown-on-discard / no-teardown-on-plain-delete / teardown-on-failed-boot / recreate-on-snp-restart / no-dhcp-on-plain-restart without running dnsmasq. | No Python oracle for the SNP path (the Python daemon has no SNP measured-boot launch); reference is the aleph-cvm donor's dnsmasq DHCP, adapted to the no-fixed-MAC SNP NIC with a single-address range instead of MAC reservations, and to a per-tap transient unit instead of one shared dnsmasq. Plain/SEV VMs are untouched (they keep cloud-init static config), so no existing behavior or test changes. Recommend nothing to upstream (this is the port direction). | keep |
| 79 | `aleph-tee` `SevSnpBackend` carries NO default C-bit parameters (D1 follow-up, completes entry 68b at the library layer; 78 is the D2 DHCP entry). D1 removed the hardcoded `cbitpos=51/reduced-phys-bits=1` from the LAUNCH path (the controller reads `SevHostInfo::read()` at launch, fail-closed) but left the pair as a constructor DEFAULT in `SevSnpBackend::new` for donor API parity. That default is now deleted: the backend stores `cbit_params: Option<CbitParams>` (`None` from `new()`), `with_cbit_params` injects the host CPUID values, and `TeeBackend::qemu_args` is now FALLIBLE (`Result<Vec<String>>`), bailing when the pair is unset, so NO caller can ever launch with a stale EPYC generation's hardcode (the same fail-closed contract the controller applies, extended to the library). `new(product)` remains parameter-free for attestation-only users: the in-guest `aleph-attest-agent` (the only production constructor call in this repo) uses report retrieval/parsing/verification and never generates argv, so the C-bit fields were inert there. The free generator `sev_snp_qemu_args` already required explicit values (D1) and is unchanged, as is the controller oracle parity. Tests: unset params fail closed with a CPUID-pointing message; an injected non-default pair (47/2) flows into the emitted `sev-snp-guest` object verbatim (also closes the D1 review nit that `with_cbit_params` lacked a direct test); `NoTeeBackend::qemu_args` wraps in `Ok`. DIVERGES from the aleph-cvm donor, whose backend still hardcodes 51/1 defaults and whose `qemu_args` is infallible: recommend UPSTREAMING together with the entry-68 host-read (the donor's launcher would read the host and inject; its default then becomes dead code). | A silent default is a launch-correctness footgun: cbitpos varies by EPYC generation and a wrong value breaks guest memory encryption, so the type system should force the host read. Making `qemu_args` fallible is the minimal API change that encodes "host values required" without burdening attestation-only constructors. | keep |
| 80 | `TeeBackend` is attestation-only: the `qemu_args` trait method is REMOVED (follow-up to entry 79, completing the launch/attestation split). The trait bundled two roles the donor never separated: attestation (get_report/parse_report/verify_report, used by the in-guest agent) and launch argv generation (qemu_args, a host-launcher concern needing host-CPUID inputs the attestation side does not have). Entry 79 modeled that union as `cbit_params: Option<CbitParams>` with a fallible qemu_args; this increment makes the invalid state unrepresentable instead: `TeeBackend::qemu_args` is deleted, `SevSnpBackend` drops its `ovmf_path`/`cbit_params` fields, `with_ovmf_path`/`with_cbit_params` builders and the `CbitParams` struct (it now carries only `product`), and `NoTeeBackend::qemu_args` goes with the trait method. The LAUNCH API is the already-explicit free generator `sev_snp::qemu::sev_snp_qemu_args(config, ovmf_path, cbitpos, reduced_phys_bits)`, which is what the supervisor-controller byte-parity oracle exercises (and what a launcher should call); its tests retain the non-default 47/2 coverage from D1, so nothing is lost by deleting the backend-level qemu_args tests. In this repo qemu_args was production-dead anyway: the controller emits its own SNP fragment (oracle-checked against the free generator) and the agent never builds argv. DIVERGES further from the aleph-cvm donor, whose `TeeBackend` keeps qemu_args and whose node daemon calls it: recommend UPSTREAMING as the same split (donor node daemon calls the free generator with host-read values), bundled with entries 68/79. | A trait method only some implementors can honestly implement is a design smell; removing it turns entry 79's runtime fail-closed guard into a compile-time impossibility, at the cost of a larger donor API divergence (recorded for upstreaming). | keep |
| 81 | `TeeBackend::verify_report` is REMOVED: the trait is now the report-PRODUCING interface only (`tee_type` + `get_report` + `parse_report`), completing the role split begun by entries 79/80. The donor's `verify_report` was a stub on every implementor: the SEV-SNP impl did structural parsing and ALWAYS returned `valid: false` (real verification is async, fetching the VCEK/ASK chain from the AMD KDS, which cannot fit the sync trait signature), and `NoTeeBackend` bailed. It had ZERO production callers: the in-guest agent is the ATTESTED party, whose verdict about its own report is worthless (a compromised guest would claim validity), so B2b (entry 72) made the verifying CLIENT the authoritative gate via the explicit async API `sev_snp::verify::verify_sev_snp_report` (ARK-pinned, entry from B0), which attest-cli drives from `SnpCertVerifier`. That API is unchanged and remains the ONLY verification path; the stub, its always-false `VerificationResult`, and the mock/`NoTeeBackend` impls are deleted (the `VerificationResult` type itself stays: it is the real verifier's return type). DIVERGES further from the aleph-cvm donor, whose trait keeps the stub: recommend UPSTREAMING together with entries 68/79/80 (the donor's stub is a latent false-negative trap at best and invites gating trust on an in-guest verdict at worst). | The attested party must not carry a verification interface: an in-guest verdict is not trustworthy, the sync signature cannot host the real async check, and a permanently-false stub is dead weight once no caller exists. Removing it makes the trait honestly describe the one role the agent has. | keep |
| 82 | The controller's SNP rootfs `-drive` token (`build_snp_argv` in `supervisor-controller/src/qemu.rs`) has NO `aleph-tee` oracle, unlike the TEE machine/object fragment (entries 79-81, `snp_tee_fragment` vs `sev_snp_qemu_args`). `aleph-tee`'s `VmConfig.encrypted` field (`crates/aleph-tee/src/types.rs`) exists but is READ BY NOTHING: `sev_snp_qemu_args` (the free generator, the only launch-argv function in the crate) emits just `-cpu`/`-machine`/`-object`x2/`-nodefaults`/`-bios` and never touches disks or `encrypted` at all. So there is no "donor argv" to diverge from for the rootfs line; the two documented shapes (default `file={},format=raw,if=virtio,readonly=on,media=disk`, and the writable luks arm `file={},format={image_format},if=virtio,media=disk` when `image_format`/`image_readonly=false` are both set) are pinned only by this repo's own conformance fixtures (`tests/conformance/controller_argv_snp/{minimal,luks}.json`) and a plain unit test, not by a cross-crate byte-parity assertion. | Investigated whether to extend the parity test with `encrypted=true` (the generator would have to emit the same drive shape); it does not emit a drive at all, so extending the oracle test would silently pass without exercising anything (the assertion would not touch the rootfs line). Recording this here instead of a no-op test. | keep |

## Deferred follow-ups (Phase 3 increment D2, need a `nix/` change + Aleph image re-upload)

The SNP measured image under `nix/` (init.sh, udhcpc.script, initrd.nix,
flake.nix) feeds the SEV-SNP launch MEASUREMENT (currently `dbf8f4ca...`,
pinned as an immutable Aleph image the CI fetches). Changing any of those files
alters the measurement and forces a re-upload of the pinned image, so the
following are recorded here rather than fixed in this batch. All are guest-side
or image-side; the attest-agent (initramfs) is unaffected, so attestation works
today.

- **Workload chroot has no DNS on the DHCP path.** RESOLVED (increment D3,
  `od/phase3-d3-guest-dns`): the verity rootfs is mounted read-only, so the
  old `echo > /mnt/root/etc/resolv.conf` could never work on measured boots
  (and `mkdir -p /mnt/root/tmp/secrets` could not create the secrets
  bind-mount target either, the same read-only failure). The rootfs now ships
  `/etc/resolv.conf` and `/tmp/secrets` placeholders, and `prepare_chroot`
  bind-mounts the initramfs `/etc/resolv.conf` (DHCP option-6 nameservers
  written by `udhcpc.script`, gateway-seeded on the static `ip=` path) over
  the placeholder. Changes the launch measurement (expected).

- **`nix/udhcpc.script` shellcheck + the non-gating CI job.** RESOLVED
  (increment D4, `od/phase3-d4-shellcheck-gate`): `# shellcheck
  disable=SC2154` documents the udhcpc-injected lease variables; the CI find
  pattern now includes `*.script`; and the job switched from `-exec shellcheck
  {} \;` (which swallowed exit codes, so shellcheck never actually failed CI)
  to `-exec ... +`, making it a real gate. The 8 pre-existing findings this
  uncovered across 5 scripts were fixed (quoting) or annotated (SC1091
  runtime source, SC2029 intentional client-side expansion).

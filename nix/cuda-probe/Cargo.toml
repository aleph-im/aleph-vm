[package]
name = "cuda-probe"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = { version = "4", default-features = false, features = ["macros"] }
anyhow = "1"
libloading = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
